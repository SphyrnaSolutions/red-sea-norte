---
phase: 03-page-templates-and-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/blocks/BlockRenderer.tsx
  - src/components/blocks/RichTextBlock.tsx
  - src/components/blocks/HeadingBlock.tsx
  - src/components/blocks/ImageBlock.tsx
  - src/components/blocks/QuoteBlock.tsx
  - src/components/blocks/InfoCardsBlock.tsx
  - src/components/blocks/GalleryBlock.tsx
  - src/components/blocks/TwoColumnBlock.tsx
  - src/components/blocks/CTABlock.tsx
  - src/components/blocks/AccordionBlock.tsx
  - src/components/blocks/NewsletterBlock.tsx
  - src/components/blocks/types.ts
  - src/components/blocks/index.ts
  - src/app/blog/[slug]/page.tsx
autonomous: true
requirements:
  - CONT-03

must_haves:
  truths:
    - "Block components are shared across all page types from a single source"
    - "Blog post pages render identically after extraction (no visual regression)"
    - "Each block component is its own file under src/components/blocks/"
  artifacts:
    - path: "src/components/blocks/BlockRenderer.tsx"
      provides: "Central block dispatcher mapping block type to component"
      exports: ["BlockRenderer"]
    - path: "src/components/blocks/index.ts"
      provides: "Barrel export for all block components"
    - path: "src/components/blocks/types.ts"
      provides: "Block type definitions shared by all renderers"
      contains: "Block"
  key_links:
    - from: "src/app/blog/[slug]/page.tsx"
      to: "src/components/blocks/BlockRenderer.tsx"
      via: "import { BlockRenderer } from '@/components/blocks'"
      pattern: "import.*BlockRenderer.*from.*@/components/blocks"
    - from: "src/components/blocks/BlockRenderer.tsx"
      to: "src/components/blocks/RichTextBlock.tsx"
      via: "BLOCK_MAP lookup"
      pattern: "rich_text.*RichTextBlock"
---

<objective>
Extract block rendering components from the blog page into a shared `src/components/blocks/` directory.

Purpose: The blog page currently defines BlockRenderer and 10 block components inline (~400 lines). These must be extracted into standalone files so the SEO article template (Plan 03-02) can reuse them without duplication. This is the prerequisite refactor before the template can be built.

Output: A `src/components/blocks/` directory with BlockRenderer, 10 block components, shared types, and barrel export. The blog page imports from the shared directory with zero visual regression.
</objective>

<execution_context>
@/home/mandycs/.claude/get-shit-done/workflows/execute-plan.md
@/home/mandycs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-page-templates-and-homepage/03-RESEARCH.md
@src/app/blog/[slug]/page.tsx
@src/lib/mock-data/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract block components into src/components/blocks/</name>
  <files>
    src/components/blocks/types.ts
    src/components/blocks/RichTextBlock.tsx
    src/components/blocks/HeadingBlock.tsx
    src/components/blocks/ImageBlock.tsx
    src/components/blocks/QuoteBlock.tsx
    src/components/blocks/InfoCardsBlock.tsx
    src/components/blocks/GalleryBlock.tsx
    src/components/blocks/TwoColumnBlock.tsx
    src/components/blocks/CTABlock.tsx
    src/components/blocks/AccordionBlock.tsx
    src/components/blocks/NewsletterBlock.tsx
    src/components/blocks/BlockRenderer.tsx
    src/components/blocks/index.ts
  </files>
  <action>
    Read `src/app/blog/[slug]/page.tsx` fully to understand all block components defined inline (BlockRenderer function at ~line 278, individual block components from ~line 305 to end of file).

    Create `src/components/blocks/types.ts`:
    - Move/import the `Block` type and all block value types (`RichTextValue`, `HeadingValue`, `ImageValue`, `QuoteValue`, `InfoCardsValue`, `GalleryValue`, `TwoColumnValue`, `CTAValue`, `AccordionValue`, `NewsletterValue`) used by the block components. Check if these are defined in `src/lib/mock-data/types.ts` or inline in the blog page. If inline, extract here. If in mock-data/types.ts, re-export from there.

    Create one file per block component, each as a named export:
    - `RichTextBlock.tsx` - renders rich text HTML via dangerouslySetInnerHTML
    - `HeadingBlock.tsx` - renders H2/H3/H4 with dynamic tag, enforces heading levels (no H1 allowed in body blocks)
    - `ImageBlock.tsx` - renders image with caption and alt using next/image
    - `QuoteBlock.tsx` - renders blockquote
    - `InfoCardsBlock.tsx` - renders info card grid
    - `GalleryBlock.tsx` - renders image gallery
    - `TwoColumnBlock.tsx` - renders two-column layout
    - `CTABlock.tsx` - renders call-to-action. If it uses `useModalStore`, mark with "use client"
    - `AccordionBlock.tsx` - renders accordion/FAQ. If uses client state, mark "use client"
    - `NewsletterBlock.tsx` - renders newsletter signup block

    Each component must:
    - Use named export: `export function XxxBlock({ block }: { block: Block }) {}`
    - Import Block type from `./types`
    - Preserve EXACT styling (Tailwind classes, inline styles, CSS variables) from the blog page
    - Use design tokens from globals.css (--font-sans, --font-display, --color-ocean-deep etc.) NOT raw hex values
    - If the component uses React hooks or event handlers, add "use client" directive

    Create `BlockRenderer.tsx`:
    - Use a `Record<string, React.ComponentType<{ block: Block }>>` map (not switch/case) for cleaner extensibility
    - Import all block components
    - Export: `export function BlockRenderer({ block }: { block: Block })`
    - Return null for unknown block types

    Create `index.ts`:
    - Barrel export: BlockRenderer, all individual block components, and types
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
    <manual>Verify each block component file exists and exports correctly</manual>
  </verify>
  <done>All 10 block components + BlockRenderer + types + index.ts exist in src/components/blocks/ with correct types and styling preserved</done>
</task>

<task type="auto">
  <name>Task 2: Refactor blog page to import shared blocks</name>
  <files>src/app/blog/[slug]/page.tsx</files>
  <action>
    Update `src/app/blog/[slug]/page.tsx`:
    1. Remove ALL inline block component definitions (BlockRenderer, RichTextBlock, HeadingBlock, ImageBlock, QuoteBlock, InfoCardsBlock, GalleryBlock, TwoColumnBlock, CTABlock, AccordionBlock, NewsletterBlock) -- approximately lines 274 to end of file.
    2. Add import at top: `import { BlockRenderer } from "@/components/blocks"`
    3. Keep ALL page-level code intact: generateStaticParams, generateMetadata, the BlogPostPage component, hero section, and the body section that maps blocks through BlockRenderer.
    4. Keep any block value type imports that the page-level code needs (if any are used outside block components).
    5. The resulting file should be ~270 lines (down from ~700).

    Verify the blog page still references BlockRenderer correctly in the JSX where it maps over `post.body`.
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && npx tsc --noEmit --pretty 2>&1 | head -30 && npx next build 2>&1 | tail -20</automated>
    <manual>Visit a blog post page and confirm blocks render with identical styling</manual>
  </verify>
  <done>Blog page imports BlockRenderer from shared blocks, file reduced to ~270 lines, TypeScript compiles, build succeeds, no visual regression</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx next build` succeeds
- `src/components/blocks/` contains 13 files (10 blocks + BlockRenderer + types + index)
- `src/app/blog/[slug]/page.tsx` no longer defines any block components inline
- Blog pages render identically (manual check)
</verification>

<success_criteria>
Block rendering is fully extracted into a shared directory. The blog page consumes shared blocks. Both the SEO article template (Plan 03-02) and the blog page use the same block components from one source.
</success_criteria>

<output>
After completion, create `.planning/phases/03-page-templates-and-homepage/03-01-SUMMARY.md`
</output>
