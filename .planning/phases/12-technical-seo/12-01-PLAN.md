---
phase: 12-technical-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
  - public/fonts/satoshi-variable.woff2
  - public/fonts/satoshi-variable-italic.woff2
  - public/fonts/clash-display-variable.woff2
  - src/lib/fonts.ts
  - src/lib/data/blog.ts
  - src/lib/data/rutas.ts
  - src/lib/data/experiencias.ts
  - src/lib/data/ofertas.ts
  - src/lib/data/cursos.ts
  - src/app/page.tsx
  - src/app/blog/page.tsx
autonomous: true
requirements: [TECH-02, TECH-03]

must_haves:
  truths:
    - "Fonts Satoshi and Clash Display load from /public/fonts/ (no external CDN requests to fontshare.com)"
    - "No FOUT: text renders with correct font on first paint (display:swap with preload via next/font)"
    - "draftMode() calls removed from all data layer fetchers"
    - "Homepage and blog listing pages use ISR with fixed revalidate (no force-dynamic)"
  artifacts:
    - path: "src/lib/fonts.ts"
      provides: "next/font/local declarations for Satoshi, Clash Display, JetBrains Mono"
      exports: ["satoshi", "clashDisplay", "jetbrainsMono"]
    - path: "public/fonts/satoshi-variable.woff2"
      provides: "Self-hosted Satoshi variable font file"
    - path: "public/fonts/clash-display-variable.woff2"
      provides: "Self-hosted Clash Display variable font file"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/lib/fonts.ts"
      via: "import font variables"
      pattern: "import.*fonts"
    - from: "src/lib/data/blog.ts"
      to: "src/lib/wagtail/fetchers.ts"
      via: "direct fetch with fixed revalidate (no draftMode)"
      pattern: "revalidate:\\s*\\d+"
---

<objective>
Self-host fonts and enable real ISR caching by removing draftMode from data fetchers.

Purpose: Eliminate FOUT from external font loading and unlock ISR so pages serve cached content instead of hitting Wagtail on every request. These two changes are independent but both affect page load performance.
Output: Self-hosted font files with next/font/local, clean data fetchers with fixed revalidate values, pages using ISR instead of force-dynamic.
</objective>

<execution_context>
@/home/mandycs/.claude/get-shit-done/workflows/execute-plan.md
@/home/mandycs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/layout.tsx
@src/app/globals.css
@src/lib/data/blog.ts
@src/lib/data/rutas.ts
@src/lib/wagtail/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Self-host Satoshi and Clash Display fonts with next/font/local</name>
  <files>
    public/fonts/satoshi-variable.woff2
    public/fonts/satoshi-variable-italic.woff2
    public/fonts/clash-display-variable.woff2
    src/lib/fonts.ts
    src/app/layout.tsx
    src/app/globals.css
  </files>
  <action>
    1. Download font files from Fontshare CDN into public/fonts/:
       - Satoshi Variable (woff2): https://api.fontshare.com/v2/css?f[]=satoshi@300,400,500,600,700,800,900 -- extract the woff2 URL from the CSS response and download the actual font file. The variable font file covers all weights 300-900.
       - Satoshi Variable Italic (woff2): same source, italic variant
       - Clash Display Variable (woff2): https://api.fontshare.com/v2/css?f[]=clash-display@200,300,400,500,600,700 -- extract and download variable woff2

    2. Create src/lib/fonts.ts with next/font/local declarations:
       - satoshi: variable font, weight 300-900, variable --font-sans, display swap
       - clashDisplay: variable font, weight 200-700, variable --font-display, display swap
       - jetbrainsMono: keep using next/font/google (JetBrains_Mono) as it's already optimized by Next.js, variable --font-mono, display swap

    3. Update src/app/layout.tsx:
       - Import satoshi, clashDisplay, jetbrainsMono from @/lib/fonts
       - Remove the JetBrains_Mono import from next/font/google (moved to fonts.ts)
       - Remove the entire <link> tags for fontshare.com (preconnect, dns-prefetch, and stylesheet)
       - Apply font CSS variables to <html> className: `${satoshi.variable} ${clashDisplay.variable} ${jetbrainsMono.variable}`

    4. Verify src/app/globals.css font-family references still match:
       - --font-display should reference 'Clash Display' (next/font/local will inject the actual font-family name)
       - --font-sans should reference 'Satoshi'
       - --font-mono should reference 'JetBrains Mono'
       - These CSS variable definitions in @theme inline may need to use the CSS variable from next/font instead of the font name string. Check Next.js docs for next/font/local CSS variable pattern.

    Note: next/font/local automatically handles preloading and prevents FOUT. No manual preload links needed.
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && ls public/fonts/*.woff2 && npx next build 2>&1 | tail -20</automated>
    <manual>Open the site in browser, check Network tab -- no requests to api.fontshare.com, fonts load from local /fonts/ path</manual>
  </verify>
  <done>Font files exist in public/fonts/, layout.tsx has no external font links, next/font/local declarations create CSS variables, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Remove draftMode from data fetchers and enable ISR on listing pages</name>
  <files>
    src/lib/data/blog.ts
    src/lib/data/rutas.ts
    src/lib/data/experiencias.ts
    src/lib/data/ofertas.ts
    src/lib/data/cursos.ts
    src/app/page.tsx
    src/app/blog/page.tsx
  </files>
  <action>
    1. In each data layer file (blog.ts, rutas.ts, experiencias.ts, ofertas.ts, cursos.ts):
       - Remove `import { draftMode } from 'next/headers'`
       - Remove `const { isEnabled: isDraft } = await draftMode()` lines
       - Replace `revalidate: isDraft ? 0 : N` with just `revalidate: N` (keep existing TTL values: blog=600, rutas=1800, experiencias=1800, ofertas=900, cursos=3600)
       - Keep the tags config unchanged (needed for on-demand revalidation in plan 02)

    2. In src/app/page.tsx (homepage):
       - Remove `export const dynamic = 'force-dynamic'` -- homepage uses mock data directly so no ISR needed, but removing force-dynamic lets Next.js statically optimize it
       - Add `export const revalidate = 600` for ISR (10 min) in case homepage migrates to CMS later

    3. In src/app/blog/page.tsx (blog listing):
       - Remove `export const dynamic = 'force-dynamic'`
       - Add `export const revalidate = 600` to match blog data layer TTL

    4. Do NOT remove draftMode from the page-level components (src/app/blog/[slug]/page.tsx etc.) -- those use draftMode for preview rendering which is a separate concern from data fetching. The preview flow still works because /api/preview sets draftMode and the page checks it for UI rendering, but the data fetchers now always use cached responses (fresh content comes via on-demand revalidation from TECH-01).

    Why this works: draftMode in fetchers was setting revalidate:0 which meant every page view hit Wagtail. With on-demand revalidation (TECH-01), pages serve cached content and only refresh when Wagtail publishes. Draft preview still works because /api/preview calls getPreviewPage which uses revalidate:0 directly in client.ts.
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && grep -r "draftMode" src/lib/data/ && echo "FAIL: draftMode still in data layer" || echo "PASS: draftMode removed from data layer" && npx next build 2>&1 | tail -20</automated>
    <manual>Verify blog listing page serves cached content (check response headers for cache status)</manual>
  </verify>
  <done>No draftMode imports in src/lib/data/*.ts files, homepage and blog listing use revalidate export instead of force-dynamic, build succeeds</done>
</task>

</tasks>

<verification>
- `ls public/fonts/*.woff2` shows at least 2 font files (Satoshi + Clash Display)
- `grep -r "fontshare" src/app/layout.tsx` returns no matches
- `grep -r "draftMode" src/lib/data/` returns no matches
- `grep -r "force-dynamic" src/app/page.tsx src/app/blog/page.tsx` returns no matches
- `npx next build` succeeds without errors
</verification>

<success_criteria>
- Satoshi and Clash Display served from public/fonts/ via next/font/local
- No external requests to fontshare.com
- All data layer fetchers use fixed revalidate values (no draftMode)
- Homepage and blog listing use ISR (revalidate export) instead of force-dynamic
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/12-technical-seo/12-01-SUMMARY.md`
</output>
