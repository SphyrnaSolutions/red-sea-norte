---
phase: 24-wreck-spot-content
plan: 03
type: execute
wave: 2
depends_on:
  - 24-01
  - 24-02
files_modified: []
autonomous: true
requirements:
  - WRECK-01
  - WRECK-02
  - WRECK-03
  - WRECK-04
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "All 11 blog posts are published and live in Wagtail CMS (accessible via API)"
    - "Each page returns HTTP 200 from Wagtail API with correct slug"
    - "Each page has BlogPosting-compatible schema fields (schema_type=Article, author, published_at)"
    - "Each page has cluster_id and cluster_role=satellite set correctly"
    - "Each page has unique primary_keyword matching CONTENT-MAP.md (no cannibalization)"
    - "Each page body renders correct HTML with internal links and external citations"
    - "Fuentes y Referencias section is present in every page body"
  artifacts: []
---

<objective>
Deploy both Phase 24 management commands to the production Wagtail CMS and verify all 11 blog posts are published correctly with proper SEO fields, cluster metadata, internal links, and authoritative citations.

Purpose: Execute the content creation commands against the live Wagtail instance to make the 11 new wreck/spot pages available on buceoenelmarrojo.com.
Output: 11 live blog posts in Wagtail CMS, verified via API.
</objective>

<context>
@.planning/phases/24-wreck-spot-content/24-01-PLAN.md (pecios command)
@.planning/phases/24-wreck-spot-content/24-02-PLAN.md (destinos command)
@../wagtail-headless-cms/apps/blog/management/commands/populate_phase24_pecios.py
@../wagtail-headless-cms/apps/blog/management/commands/populate_phase24_destinos.py
@.env.example (Wagtail URL: https://back.redsea.sphyrnasolutions.com)
</context>

<tasks>
<task type="auto">
  <name>Run populate_phase24_pecios.py on production Wagtail</name>
  <files></files>
  <action>
Execute the pecios management command against the Wagtail CMS:

1. cd to wagtail-headless-cms directory
2. Activate the Python venv
3. First run with --dry-run to verify: python manage.py populate_phase24_pecios --dry-run
4. If dry-run succeeds, run without --dry-run: python manage.py populate_phase24_pecios
5. Verify output shows 5 posts created

If the command fails due to missing Author or Category, check if they exist from populate_blog.py run (they should -- "Carlos Martinez" author and "Pecios"/"Spots" categories were created in Phase 8).
  </action>
  <verify>
    - Command exits with status 0
    - Output shows 5 blog posts created (or skipped if already exist)
  </verify>
  <done>5 pecios blog posts deployed to Wagtail CMS</done>
</task>

<task type="auto">
  <name>Run populate_phase24_destinos.py on production Wagtail</name>
  <files></files>
  <action>
Execute the destinos management command:

1. cd to wagtail-headless-cms directory (if not already there)
2. First --dry-run: python manage.py populate_phase24_destinos --dry-run
3. Then run: python manage.py populate_phase24_destinos
4. Verify output shows 6 posts created
  </action>
  <verify>
    - Command exits with status 0
    - Output shows 6 blog posts created
  </verify>
  <done>6 destinos blog posts deployed to Wagtail CMS</done>
</task>

<task type="auto">
  <name>Verify all 11 pages via Wagtail API</name>
  <files></files>
  <action>
For each of the 11 slugs, verify the page exists and has correct fields by querying the Wagtail API:

Expected slugs:
1. tile-wreck-abu-nuhas-guia-buceo
2. kimon-m-abu-nuhas-historia-buceo
3. marcus-abu-nuhas-pecio-desconocido
4. bucear-ss-thistlegorm-interior-cubierta
5. estrecho-gubal-pecios-arrecifes-buceo
6. anemone-city-ras-mohammed-guia-buceo
7. jackson-reef-gordon-reef-tiran-buceo
8. erg-abu-ramada-acuario-mar-rojo
9. shaab-el-erg-delfines-hurghada-buceo
10. small-crack-big-crack-mar-rojo-buceo
11. vida-marina-mar-rojo-especies-buceo

For each page verify via curl to Wagtail API:
curl -s "https://back.redsea.sphyrnasolutions.com/api/v2/pages/?type=blog.BlogPostPage&slug={slug}&fields=title,primary_keyword,cluster_id,cluster_role,schema_type" | jq '.items[0] | {title, primary_keyword, cluster_id, cluster_role, schema_type}'

Check:
- Each slug returns exactly 1 result
- primary_keyword matches CONTENT-MAP.md
- cluster_role = "satellite"
- schema_type = "Article"

Also verify body content for a sample (first 3 slugs):
curl -s "https://back.redsea.sphyrnasolutions.com/api/v2/pages/?type=blog.BlogPostPage&slug={slug}&fields=*" | jq '.items[0].body | length'
- Body should have multiple blocks (>5)

Check that internal links are present:
curl -s "https://back.redsea.sphyrnasolutions.com/api/v2/pages/?type=blog.BlogPostPage&slug=tile-wreck-abu-nuhas-guia-buceo&fields=*" | jq -r '.items[0].body[] | select(.type=="rich_text") | .value' | grep -c 'href="/blog\|href="/rutas'
- Should return >= 2 internal links

Check that authoritative citations are present:
curl ... | grep -c 'href="https://'
- Should return >= 2 external links per page
  </action>
  <verify>
    - All 11 slugs return data from API
    - All pages have correct cluster metadata
    - Body content has multiple blocks
    - Internal links present
    - External citation links present
  </verify>
  <done>All 11 Phase 24 pages verified live in Wagtail CMS</done>
</task>

<task type="auto">
  <name>Trigger ISR revalidation for new blog pages</name>
  <files></files>
  <action>
Trigger Next.js ISR revalidation so the new pages appear on the frontend:

For each of the 11 new slugs, call the revalidation endpoint:
curl -X POST "https://buceoenelmarrojo.com/api/revalidate?secret=${REVALIDATION_SECRET}&path=/blog/{slug}"

Also revalidate the blog listing page:
curl -X POST "https://buceoenelmarrojo.com/api/revalidate?secret=${REVALIDATION_SECRET}&path=/blog"

Read the REVALIDATION_SECRET from the Dokploy environment or .env file.

If revalidation endpoint is not available or fails, the pages will still appear after the ISR cache expires (1 hour default).
  </action>
  <verify>
    - Revalidation requests return 200 or pages are accessible via frontend
    - curl https://buceoenelmarrojo.com/blog/tile-wreck-abu-nuhas-guia-buceo returns 200
  </verify>
  <done>ISR cache revalidated, new pages live on frontend</done>
</task>
</tasks>
