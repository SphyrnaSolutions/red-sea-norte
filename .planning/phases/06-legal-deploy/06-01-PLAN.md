---
phase: 06-legal-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/politica-de-privacidad/page.tsx
  - src/components/organisms/Footer.tsx
  - docker-compose.yml
  - .env.example
autonomous: false
requirements:
  - LEGAL-01

user_setup:
  - service: odoo
    why: "CRM lead management -- Odoo env vars needed at runtime for API route"
    env_vars:
      - name: ODOO_URL
        source: "Odoo instance URL (e.g., https://karlos-odoo.example.com)"
      - name: ODOO_DB
        source: "Odoo database name from Settings -> Database Name"
      - name: ODOO_API_KEY
        source: "Odoo Settings -> Users -> API Keys -> generate new key for dedicated CRM user"
    dashboard_config:
      - task: "Add ODOO_URL, ODOO_DB, ODOO_API_KEY to Dokploy environment variables or docker-compose.yml on server"
        location: "Dokploy dashboard -> Service -> Environment or server docker-compose.yml"

must_haves:
  truths:
    - "Visiting /politica-de-privacidad renders a privacy policy page in Spanish"
    - "Privacy page covers: data collected (name, email, phone, certification, month), purpose (lead management for diving trips), WhatsApp marketing with consent, data controller info, GDPR user rights (access, rectification, erasure, portability)"
    - "Footer link 'Politica de Privacidad' navigates to /politica-de-privacidad"
    - "docker-compose.yml includes ODOO_URL, ODOO_DB, ODOO_API_KEY as runtime environment variables"
  artifacts:
    - path: "src/app/politica-de-privacidad/page.tsx"
      provides: "Privacy policy page with GDPR-compliant content"
      min_lines: 80
    - path: "src/components/organisms/Footer.tsx"
      provides: "Updated footer link pointing to /politica-de-privacidad"
      contains: "/politica-de-privacidad"
    - path: "docker-compose.yml"
      provides: "Odoo runtime env vars for API route"
      contains: "ODOO_URL"
  key_links:
    - from: "src/components/organisms/Footer.tsx"
      to: "/politica-de-privacidad"
      via: "Next.js Link component"
      pattern: "href.*politica-de-privacidad"
    - from: "docker-compose.yml"
      to: "src/app/api/ (future lead route)"
      via: "runtime environment variables"
      pattern: "ODOO_URL|ODOO_DB|ODOO_API_KEY"
---

<objective>
Create the privacy policy page, fix footer link, and configure Odoo environment variables for production deployment.

Purpose: LEGAL-01 compliance (privacy policy with GDPR and WhatsApp marketing consent info) and deployment readiness for the lead capture API route.
Output: Privacy policy page at /politica-de-privacidad, updated footer link, docker-compose.yml with Odoo env vars.
</objective>

<execution_context>
@/home/mandycs/.claude/get-shit-done/workflows/execute-plan.md
@/home/mandycs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/organisms/Footer.tsx
@docker-compose.yml
@.env.example
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create privacy policy page and fix footer link</name>
  <files>
    src/app/politica-de-privacidad/page.tsx
    src/components/organisms/Footer.tsx
  </files>
  <action>
    Create `src/app/politica-de-privacidad/page.tsx` as a server component (no "use client") with:

    1. **Metadata export**: title "Politica de Privacidad | Buceo en el Mar Rojo", description about data treatment.

    2. **Page content in Spanish** covering these sections (use semantic HTML with Tailwind styling consistent with the site's design system -- ocean-deep, coral-fire color palette, Clash Display headings, Satoshi body):

    - **Responsable del tratamiento**: Business name "Buceo en el Mar Rojo" (buceoenelmarrojo.com), based in Hurghada, Egypt. Contact email (use info@buceoenelmarrojo.com as placeholder).

    - **Datos que recopilamos**: Name (nombre), email, phone (telefono), diving certification level (certificacion), preferred travel month (mes preferido). Explain these are collected via the lead capture form.

    - **Finalidad del tratamiento**: Managing leads for diving trip inquiries from Hurghada/Red Sea. Contacting users about their diving trip interest.

    - **Marketing por WhatsApp**: Explain that with explicit consent (checkbox in form), the user agrees to receive promotional messages via WhatsApp about diving trips, offers, and related content. Include: business name sending messages, message type (promotional/informational about diving), WhatsApp as the channel, how to opt-out (reply STOP or contact email). Reference Meta's messaging policies compliance.

    - **Base legal**: Consent (Art. 6.1.a GDPR) for form submission and WhatsApp marketing. Legitimate interest for responding to inquiries.

    - **Derechos del usuario (GDPR)**: Access (acceso), rectification (rectificacion), erasure (supresion), data portability (portabilidad), restriction of processing (limitacion), objection (oposicion). How to exercise: email to the controller.

    - **Conservacion de datos**: Data retained while business relationship exists or as legally required. WhatsApp consent can be withdrawn at any time.

    - **Destinatarios**: Data stored in Odoo CRM. No third-party sharing except as required by law.

    - **Fecha de actualizacion**: Show "Ultima actualizacion: marzo 2026".

    3. **Styling**: Use a clean, readable layout. Max-width prose container (max-w-3xl mx-auto). H1 for page title. H2 for each section. Good spacing between sections (space-y-8 or similar). Text color using the site's palette. Background consistent with other pages. Do NOT use the anti-AI-slop font rules for this page -- it's a legal page that needs readability over aesthetics.

    4. **Fix Footer.tsx**: Change the privacy link href from `/privacidad` to `/politica-de-privacidad`. Keep the label "Politica de Privacidad".
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && npx next build 2>&1 | tail -20</automated>
    <manual>Visit /politica-de-privacidad in dev mode and verify all sections render correctly</manual>
  </verify>
  <done>
    /politica-de-privacidad renders a complete privacy policy page in Spanish covering all required sections (data collected, purpose, WhatsApp marketing, GDPR rights, data controller). Footer link points to /politica-de-privacidad instead of /privacidad.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Odoo runtime env vars to docker-compose and .env.example</name>
  <files>
    docker-compose.yml
    .env.example
  </files>
  <action>
    1. **Update docker-compose.yml**: Add Odoo environment variables to the `frontend` service's `environment` section. These are server-side only (NOT prefixed with NEXT_PUBLIC_), so they work as runtime env vars -- no need to be present at build time:

    ```yaml
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_WAGTAIL_URL=${NEXT_PUBLIC_WAGTAIL_URL}
      - NEXT_PUBLIC_WAGTAIL_API_URL=${NEXT_PUBLIC_WAGTAIL_API_URL}
      - NEXT_PUBLIC_SITE_HOSTNAME=${NEXT_PUBLIC_SITE_HOSTNAME}
      - NEXT_PUBLIC_SITE_URL=${NEXT_PUBLIC_SITE_URL}
      - ODOO_URL=${ODOO_URL}
      - ODOO_DB=${ODOO_DB}
      - ODOO_API_KEY=${ODOO_API_KEY}
    ```

    Do NOT add these to the Dockerfile builder stage -- they are runtime-only and must NOT be baked into the image.

    2. **Update .env.example**: Add the three Odoo variables with descriptive comments:

    ```
    # Odoo CRM Integration (server-side only, runtime)
    ODOO_URL=https://your-odoo-instance.com
    ODOO_DB=your-database-name
    ODOO_API_KEY=your-api-key
    ```
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && grep -c "ODOO_" docker-compose.yml && grep -c "ODOO_" .env.example</automated>
    <manual>Verify docker-compose.yml has ODOO_URL, ODOO_DB, ODOO_API_KEY in environment section and .env.example documents them</manual>
  </verify>
  <done>
    docker-compose.yml includes ODOO_URL, ODOO_DB, ODOO_API_KEY as runtime environment variables in the frontend service. .env.example documents all three Odoo variables with comments.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify privacy page and deployment config</name>
  <files>none -- verification only</files>
  <action>
    Human verification checkpoint. Review the privacy policy page and deployment configuration created by Tasks 1 and 2.

    What was built:
    - Privacy policy page at /politica-de-privacidad with full GDPR-compliant content
    - Updated footer link from /privacidad to /politica-de-privacidad
    - Odoo env vars (ODOO_URL, ODOO_DB, ODOO_API_KEY) added to docker-compose.yml

    How to verify:
    1. Run `cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && npm run dev`
    2. Visit http://localhost:3000/politica-de-privacidad -- verify all privacy sections render (data collected, purpose, WhatsApp marketing, GDPR rights, data controller)
    3. Scroll to footer on any page -- click "Politica de Privacidad" link -- should navigate to /politica-de-privacidad
    4. Review docker-compose.yml -- confirm ODOO_URL, ODOO_DB, ODOO_API_KEY are in the environment section
    5. Verify the WhatsApp marketing section mentions: business name, message type, channel (WhatsApp), opt-out method

    Resume signal: Type "approved" or describe issues to fix.
  </action>
  <verify>
    <automated>echo "checkpoint -- human verification required"</automated>
  </verify>
  <done>
    User has verified that the privacy policy page content is correct and complete, footer link works, and docker-compose.yml has the Odoo env vars.
  </done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. /politica-de-privacidad page exists and renders privacy content
3. Footer link points to /politica-de-privacidad
4. docker-compose.yml contains ODOO_URL, ODOO_DB, ODOO_API_KEY
5. .env.example documents Odoo variables
</verification>

<success_criteria>
- Privacy policy page renders at /politica-de-privacidad with all required legal content
- Footer navigation updated to link to correct privacy page URL
- Odoo environment variables configured for runtime injection in production
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-legal-deploy/06-01-SUMMARY.md`
</output>
