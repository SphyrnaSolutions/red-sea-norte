---
phase: 19-routing-dead-links
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/contacto/page.tsx
  - src/components/organisms/Footer.tsx
  - next.config.ts
autonomous: true
requirements: [ROUTE-01, ROUTE-02, ROUTE-03]

must_haves:
  truths:
    - "/contacto returns HTTP 200 with a functional contact form visible in server HTML"
    - "Visiting /blog/ss-thistlegorm-gu%C3%ADa-completa redirects 301 to /blog/ss-thistlegorm-guia-completa"
    - "Visiting /blog/ras-mohammed-gu%C3%ADa-completa redirects 301 to /blog/ras-mohammed-guia-completa"
    - "Footer contains no link to /ofertas"
    - "Footer contains no link to /terminos"
  artifacts:
    - path: "src/app/contacto/page.tsx"
      provides: "Contact page with inline lead form"
      min_lines: 40
    - path: "src/components/organisms/Footer.tsx"
      provides: "Footer without dead links"
    - path: "next.config.ts"
      provides: "301 redirects for accented blog slugs"
  key_links:
    - from: "src/app/contacto/page.tsx"
      to: "/api/lead"
      via: "InlineLeadSection component"
      pattern: "InlineLeadSection"
    - from: "next.config.ts"
      to: "src/app/blog/[slug]/page.tsx"
      via: "301 redirect from accented slug to ASCII slug"
      pattern: "thistlegorm-gu.*completa"
---

<objective>
Fix all broken routing and dead links: create the missing /contacto page, add 301 redirects for blog URLs with accented slugs, and remove dead footer links to /ofertas and /terminos.

Purpose: Eliminate all 404s from linked pages -- /contacto is the main conversion page and must work, accented blog URLs appear in sitemaps/internal links and must resolve, footer must not link to nonexistent pages.
Output: Working /contacto page, 301 redirects in next.config.ts, cleaned Footer.tsx
</objective>

<execution_context>
@/home/mandycs/.claude/get-shit-done/workflows/execute-plan.md
@/home/mandycs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/components/organisms/Footer.tsx
@src/components/organisms/InlineLeadSection.tsx
@src/components/organisms/LeadFormModal.tsx
@src/lib/mock-data/homepage.ts
@src/app/page.tsx
@next.config.ts
@src/app/blog/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /contacto page with inline lead form</name>
  <files>src/app/contacto/page.tsx</files>
  <action>
Create `src/app/contacto/page.tsx` as a server component that renders a dedicated contact page. This page does NOT exist yet (confirmed via file search -- that is why /contacto returns 404).

The page must:

1. Export `generateMetadata()` returning title "Contacto | Buceo en el Mar Rojo", description about contacting for diving liveaboard info, canonical URL `https://buceoenelmarrojo.com/contacto`, and appropriate og/twitter tags.

2. Import and render `InlineLeadSection` (from `@/components/organisms/InlineLeadSection`) with these props -- follow the exact same pattern as the homepage (`src/app/page.tsx` line 103, `src/lib/mock-data/homepage.ts` lines 357-375):
   - sectionId: "contacto-form"
   - eyebrow: "CONTACTO"
   - title: "Cuentanos sobre tu viaje al Mar Rojo"
   - subtitle: "Rellena el formulario y te contactaremos para resolver tus dudas sobre rutas, fechas y disponibilidad."
   - highlights: ["Te orientamos segun tu nivel de buceo", "Te contamos que ruta encaja mejor contigo", "Sin compromiso, respuesta en menos de 24h"]
   - fields: same baseLeadFields pattern as homepage (name, email, phone, certification select, preferredMonth select) -- import or inline the field definitions
   - submitButton: "Enviar consulta"
   - privacyText: ""
   - successMessage: "Gracias. Te contactaremos pronto con informacion personalizada."
   - consentText: "Acepto recibir ofertas y novedades sobre viajes de buceo por WhatsApp de buceoenelmarrojo.com. Puedo darme de baja en cualquier momento."
   - privacyLinkText: "Politica de privacidad"
   - privacyLinkHref: "/politica-de-privacidad"
   - showModalCta: false (no need for secondary modal on a dedicated contact page)

3. Above the form, render a simple hero/header section with an H1 "Contacto" and brief intro text. Use the same dark navy background pattern as other pages (#0A2540 or similar). Keep it simple -- no hero image needed.

4. Also import and render `LeadFormModal` (from `@/components/organisms/LeadFormModal`) so the modal CTA still works if triggered from other components on the page. Pass same fields as InlineLeadSection.

5. Add JSON-LD ContactPage schema with breadcrumbs.

Do NOT use `export const dynamic = 'force-dynamic'` -- this is a static page with no data fetching.
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && npx next build 2>&1 | grep -E "(error|contacto)" | head -20</automated>
    <manual>Visit https://buceoenelmarrojo.com/contacto and verify form renders with all fields</manual>
  </verify>
  <done>/contacto returns HTTP 200 with visible contact form containing name, email, phone, certification, month fields, WhatsApp consent checkbox, and submit button. Form submits to /api/lead successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Add 301 redirects for accented blog slugs and clean footer dead links</name>
  <files>next.config.ts, src/components/organisms/Footer.tsx</files>
  <action>
**next.config.ts -- Add accented slug redirects:**

In the existing `redirects()` array, add two new entries AFTER the existing merged post redirects:

```
{ source: '/blog/ss-thistlegorm-gu%C3%ADa-completa', destination: '/blog/ss-thistlegorm-guia-completa', permanent: true },
{ source: '/blog/ras-mohammed-gu%C3%ADa-completa', destination: '/blog/ras-mohammed-guia-completa', permanent: true },
```

The `%C3%AD` is the URL-encoded form of "i" (i with accent). Next.js redirects match URL-encoded paths. The destination uses the ASCII slug that Wagtail/mock-data actually serves.

IMPORTANT: Also add the non-encoded version as a second pair, because browsers may send the raw UTF-8:
```
{ source: '/blog/ss-thistlegorm-guía-completa', destination: '/blog/ss-thistlegorm-guia-completa', permanent: true },
{ source: '/blog/ras-mohammed-guía-completa', destination: '/blog/ras-mohammed-guia-completa', permanent: true },
```

This covers both encoded and raw UTF-8 requests.

**Footer.tsx -- Remove dead links:**

1. In the `footerLinks.explora` array (line 8), remove the entry `{ label: "Ofertas", href: "/ofertas" }`. The /ofertas listing page does not exist (only /ofertas/[slug] exists and has no content). Keep "Rutas" and "Experiencias".

2. In the bottom bar (line 140), remove the entire `<Link href="/terminos">` element. There is no /terminos page. Keep only the "Politica de Privacidad" link.

3. Add "Contacto" link to the `footerLinks.compania` array: `{ label: "Contacto", href: "/contacto" }` -- this links to the new page created in Task 1.

Do NOT change Footer layout, styling, or any other links.
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && npx next build 2>&1 | tail -5 && grep -c "ofertas\|terminos" src/components/organisms/Footer.tsx</automated>
    <manual>Check footer has no /ofertas or /terminos links. Visit accented blog URL and verify 301 redirect.</manual>
  </verify>
  <done>Footer contains links to Rutas, Experiencias, Blog, Contacto, and Politica de Privacidad only -- no /ofertas, no /terminos. Accented blog URLs return 301 to ASCII versions.</done>
</task>

</tasks>

<verification>
1. `curl -sI https://buceoenelmarrojo.com/contacto` returns HTTP 200
2. `curl -sI 'https://buceoenelmarrojo.com/blog/ss-thistlegorm-gu%C3%ADa-completa'` returns HTTP 301 with Location: /blog/ss-thistlegorm-guia-completa
3. `curl -sI 'https://buceoenelmarrojo.com/blog/ras-mohammed-gu%C3%ADa-completa'` returns HTTP 301 with Location: /blog/ras-mohammed-guia-completa
4. `grep -c '/ofertas\|/terminos' src/components/organisms/Footer.tsx` returns 0
5. Build succeeds with no errors
</verification>

<success_criteria>
- /contacto serves HTTP 200 with functional lead capture form (name, email, phone, certification, month, WhatsApp consent)
- /blog/ss-thistlegorm-guia-completa (ASCII) still serves content normally
- /blog/ss-thistlegorm-guia-completa with accent redirects 301 to ASCII version
- /blog/ras-mohammed-guia-completa with accent redirects 301 to ASCII version
- Footer "Explora" section has Rutas and Experiencias only (no Ofertas)
- Footer bottom bar has Politica de Privacidad only (no Terminos y Condiciones)
- Footer "Compania" section includes Contacto link
- Next.js build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/19-routing-dead-links/19-01-SUMMARY.md`
</output>
