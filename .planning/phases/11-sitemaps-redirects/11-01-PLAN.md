---
phase: 11-sitemaps-redirects
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/sitemap.ts
  - src/app/sitemap/blog/route.ts
  - src/app/sitemap/rutas/route.ts
  - src/app/sitemap/ofertas/route.ts
  - src/app/sitemap/cursos/route.ts
  - src/app/sitemap/experiencias/route.ts
  - src/app/sitemap/pages/route.ts
  - src/app/sitemap/images/route.ts
  - src/app/robots.ts
  - next.config.ts
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-03
  - SITE-04

must_haves:
  truths:
    - "/sitemap.xml returns a sitemap index linking to per-category sitemaps (blog, rutas, ofertas, cursos, experiencias, pages)"
    - "Each per-category sitemap returns valid XML with real URLs from Wagtail CMS"
    - "/blog/giannis-d-abu-nuhas, /blog/dunraven-mar-rojo, /blog/carnatic-abu-nuhas, /blog/camarotes-comida-wifi-liveaboard, /blog/shark-yolanda-reef-mar-rojo return 301 redirects"
    - "Merged post slugs do NOT appear in any sitemap XML output"
    - "An image sitemap exists listing indexable images with loc and image:loc elements"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "Sitemap index pointing to per-category sitemaps"
      contains: "sitemapIndex"
    - path: "src/app/sitemap/blog/route.ts"
      provides: "Blog-specific sitemap XML excluding merged posts"
      exports: ["GET"]
    - path: "src/app/sitemap/images/route.ts"
      provides: "Image sitemap for Google Images indexing"
      exports: ["GET"]
    - path: "next.config.ts"
      provides: "301 redirects for 5 merged posts"
      contains: "redirects"
  key_links:
    - from: "src/app/sitemap.ts"
      to: "/sitemap/blog, /sitemap/rutas, etc."
      via: "sitemap index URLs"
      pattern: "sitemap.*blog"
    - from: "src/app/sitemap/blog/route.ts"
      to: "src/lib/data/blog.ts"
      via: "getAllBlogPostSlugsData"
      pattern: "getAllBlogPostSlugsData"
    - from: "next.config.ts"
      to: "merged post slugs"
      via: "redirects config"
      pattern: "giannis-d|dunraven|carnatic|camarotes-comida-wifi|shark-yolanda"
---

<objective>
Convert the single sitemap.xml into a sitemap index with granular per-category sitemaps, add 301 redirects for 5 merged blog posts, exclude those merged posts from sitemaps, and create an image sitemap for Google Images.

Purpose: Search engines receive focused sitemaps for better crawl budget, merged posts redirect cleanly without 404s or index pollution, and images become discoverable in Google Images.
Output: Sitemap index at /sitemap.xml, 6 category sitemaps, 1 image sitemap, 5 redirects in next.config.ts
</objective>

<execution_context>
@/home/mandycs/.claude/get-shit-done/workflows/execute-plan.md
@/home/mandycs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/sitemap.ts
@src/lib/data/blog.ts
@src/lib/data/index.ts
@src/lib/wagtail/types.ts
@next.config.ts
@src/app/robots.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sitemap index + per-category sitemaps + image sitemap</name>
  <files>
    src/app/sitemap.ts
    src/app/sitemap/blog/route.ts
    src/app/sitemap/rutas/route.ts
    src/app/sitemap/ofertas/route.ts
    src/app/sitemap/cursos/route.ts
    src/app/sitemap/experiencias/route.ts
    src/app/sitemap/pages/route.ts
    src/app/sitemap/images/route.ts
    src/app/robots.ts
  </files>
  <action>
    1. Convert `src/app/sitemap.ts` from a flat sitemap into a sitemap index. Use Next.js `MetadataRoute.Sitemap` with the generateSitemaps() pattern OR manually return XML. The sitemap index at /sitemap.xml must link to:
       - /sitemap/blog (blog posts)
       - /sitemap/rutas (rutas)
       - /sitemap/ofertas (ofertas)
       - /sitemap/cursos (cursos)
       - /sitemap/experiencias (experiencias)
       - /sitemap/pages (static pages: homepage, /blog listing, /politica-de-privacidad)
       - /sitemap/images (image sitemap)

    2. Create route handlers for each category sitemap under `src/app/sitemap/[category]/route.ts`. Each must:
       - Export a GET handler returning XML with `Content-Type: application/xml`
       - Fetch slugs from the corresponding data function (e.g., `getAllBlogPostSlugsData()`)
       - Generate valid sitemap XML with `<urlset>`, `<url>`, `<loc>`, `<lastmod>`, `<changefreq>`, `<priority>`
       - Use BASE_URL from `process.env.NEXT_PUBLIC_SITE_URL || 'https://buceoenelmarrojo.com'`

    3. In the blog sitemap (`src/app/sitemap/blog/route.ts`), EXCLUDE the 5 merged post slugs. Define a constant:
       ```typescript
       const MERGED_POST_SLUGS = [
         'giannis-d-abu-nuhas',
         'dunraven-mar-rojo',
         'carnatic-abu-nuhas',
         'camarotes-comida-wifi-liveaboard',
         'shark-yolanda-reef-mar-rojo',
       ]
       ```
       Filter these out before generating XML.

    4. Create the image sitemap at `src/app/sitemap/images/route.ts`:
       - Fetch all blog posts with hero_image data (use `getAllBlogPostsData()` to get full post objects including hero_image)
       - Also fetch rutas, ofertas, experiencias, cursos that have images
       - Generate XML using the Google Image Sitemap extension namespace: `xmlns:image="http://www.google.com/schemas/sitemap-image/1.1"`
       - Each `<url>` contains `<image:image><image:loc>URL</image:loc></image:image>` for the page's hero image
       - Only include posts/pages that actually have a hero_image with a valid URL
       - Exclude merged post slugs from image sitemap too

    5. Update `src/app/robots.ts` to reference the sitemap index URL (should still point to /sitemap.xml which is now the index).

    IMPORTANT: Use raw XML string generation with template literals for the route handlers (not Next.js MetadataRoute types which don't support image sitemaps or the index pattern well). Return `new Response(xml, { headers: { 'Content-Type': 'application/xml' } })`.
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && npx next build 2>&1 | tail -20</automated>
    <manual>After deploy: curl https://buceoenelmarrojo.com/sitemap.xml should show sitemap index, each linked sitemap should return valid XML, merged slugs should not appear in /sitemap/blog</manual>
  </verify>
  <done>
    - /sitemap.xml returns sitemap index with links to 7 category sitemaps
    - Each category sitemap returns valid XML with real URLs
    - Blog sitemap excludes the 5 merged post slugs
    - Image sitemap lists hero images with Google image extension namespace
    - robots.ts still points to /sitemap.xml
  </done>
</task>

<task type="auto">
  <name>Task 2: 301 redirects for merged blog posts</name>
  <files>
    next.config.ts
  </files>
  <action>
    Add an `async redirects()` function to `next.config.ts` that returns 301 permanent redirects for the 5 merged posts. The redirect mappings are (based on the Phase 9 audit merge plan):

    | Source slug | Destination slug | Rationale |
    |-------------|-----------------|-----------|
    | /blog/giannis-d-abu-nuhas | /blog/abu-nuhas-cementerio-de-barcos | Giannis D merged into Abu Nuhas |
    | /blog/dunraven-mar-rojo | /blog/abu-nuhas-cementerio-de-barcos | Dunraven merged into Abu Nuhas |
    | /blog/carnatic-abu-nuhas | /blog/abu-nuhas-cementerio-de-barcos | Carnatic merged into Abu Nuhas |
    | /blog/camarotes-comida-wifi-liveaboard | /blog/como-es-un-vida-a-bordo-mar-rojo | Camarotes merged into vida-a-bordo |
    | /blog/shark-yolanda-reef-mar-rojo | /blog/ras-mohammed-guia-completa | Shark/Yolanda merged into Ras Mohammed |

    Implementation:
    ```typescript
    async redirects() {
      return [
        { source: '/blog/giannis-d-abu-nuhas', destination: '/blog/abu-nuhas-cementerio-de-barcos', permanent: true },
        { source: '/blog/dunraven-mar-rojo', destination: '/blog/abu-nuhas-cementerio-de-barcos', permanent: true },
        { source: '/blog/carnatic-abu-nuhas', destination: '/blog/abu-nuhas-cementerio-de-barcos', permanent: true },
        { source: '/blog/camarotes-comida-wifi-liveaboard', destination: '/blog/como-es-un-vida-a-bordo-mar-rojo', permanent: true },
        { source: '/blog/shark-yolanda-reef-mar-rojo', destination: '/blog/ras-mohammed-guia-completa', permanent: true },
      ]
    },
    ```

    Add this inside the `nextConfig` object alongside the existing `headers()` and `images` properties. `permanent: true` generates HTTP 301.
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && npx next build 2>&1 | tail -5 && echo "--- Redirect config check ---" && node -e "const c = require('./next.config.ts'); console.log('has redirects:', typeof c.default?.redirects === 'function' || 'check ts')"</automated>
    <manual>After deploy: curl -I https://buceoenelmarrojo.com/blog/giannis-d-abu-nuhas should return 301 with Location header pointing to /blog/abu-nuhas-cementerio-de-barcos</manual>
  </verify>
  <done>
    - All 5 merged post URLs return HTTP 301 with correct destination
    - Build succeeds with redirects configured
    - Existing headers() and images config remain unchanged
  </done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. /sitemap.xml returns XML with `<sitemapindex>` containing 7 `<sitemap>` entries
3. /sitemap/blog returns XML without any of the 5 merged slugs
4. /sitemap/images returns XML with `image:` namespace and image URLs
5. /blog/giannis-d-abu-nuhas returns 301 redirect
6. robots.txt still references /sitemap.xml
</verification>

<success_criteria>
- Sitemap index serves 7 category sitemaps at /sitemap.xml
- Each category sitemap generates valid XML from Wagtail data
- 5 merged posts excluded from all sitemaps
- 5 merged posts return 301 to correct destinations
- Image sitemap uses Google image extension namespace
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-sitemaps-redirects/11-01-SUMMARY.md`
</output>
