---
phase: 02-seo-content-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../wagtail-headless-cms/apps/core/models.py
  - ../wagtail-headless-cms/apps/rutas/models.py
  - ../wagtail-headless-cms/apps/blog/models.py
  - ../wagtail-headless-cms/apps/experiencias/models.py
  - ../wagtail-headless-cms/apps/cursos/models.py
  - ../wagtail-headless-cms/apps/ofertas/models.py
  - src/lib/wagtail/types.ts
autonomous: true
requirements: [CONT-01, CONT-02]

must_haves:
  truths:
    - "Every Wagtail page model has cluster metadata fields (cluster_id, cluster_role, pillar_slug, schema_type, primary_keyword)"
    - "primary_keyword has a unique constraint at the database level to prevent keyword cannibalization"
    - "All new fields are exposed via Wagtail API (APIField entries)"
    - "TypeScript types in the Next.js frontend match the new Wagtail API fields"
  artifacts:
    - path: "../wagtail-headless-cms/apps/core/models.py"
      provides: "ClusterMixin abstract model with cluster_id, cluster_role, pillar_slug, schema_type, primary_keyword fields"
      contains: "class ClusterMixin"
    - path: "src/lib/wagtail/types.ts"
      provides: "WagtailClusterFields interface and extended page types"
      contains: "WagtailClusterFields"
  key_links:
    - from: "../wagtail-headless-cms/apps/rutas/models.py"
      to: "../wagtail-headless-cms/apps/core/models.py"
      via: "ClusterMixin inheritance"
      pattern: "ClusterMixin"
    - from: "src/lib/wagtail/types.ts"
      to: "../wagtail-headless-cms/apps/core/models.py"
      via: "TypeScript types matching Python fields"
      pattern: "cluster_id.*cluster_role.*pillar_slug"
---

<objective>
Extend Wagtail page models with cluster metadata and update Next.js TypeScript types to match.

Purpose: All downstream SEO infrastructure (cluster resolver, interlink engine, schema markup) depends on pages having cluster_id, cluster_role, pillar_slug, schema_type, and primary_keyword fields. This plan creates the data foundation.
Output: ClusterMixin in Wagtail, all page models extended, migration files created, TypeScript types updated.
</objective>

<execution_context>
@/home/mandycs/.claude/get-shit-done/workflows/execute-plan.md
@/home/mandycs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-seo-content-pipeline/02-RESEARCH.md

# Wagtail codebase (sibling directory)
@../wagtail-headless-cms/apps/core/models.py
@../wagtail-headless-cms/apps/rutas/models.py
@../wagtail-headless-cms/apps/blog/models.py
@../wagtail-headless-cms/apps/experiencias/models.py
@../wagtail-headless-cms/apps/cursos/models.py
@../wagtail-headless-cms/apps/ofertas/models.py

# Next.js types
@src/lib/wagtail/types.ts
@src/lib/wagtail/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ClusterMixin and extend all Wagtail page models</name>
  <files>
    ../wagtail-headless-cms/apps/core/models.py
    ../wagtail-headless-cms/apps/rutas/models.py
    ../wagtail-headless-cms/apps/blog/models.py
    ../wagtail-headless-cms/apps/experiencias/models.py
    ../wagtail-headless-cms/apps/cursos/models.py
    ../wagtail-headless-cms/apps/ofertas/models.py
  </files>
  <action>
    1. In `apps/core/models.py`, create a `ClusterMixin(models.Model)` abstract model with these fields:
       - `cluster_id` CharField(max_length=100, blank=True) -- cluster identifier like "vida-a-bordo", "ruta-norte"
       - `cluster_role` CharField(max_length=20, blank=True, choices=[('pillar','Pillar'),('satellite','Satellite')]) -- page's role in cluster
       - `pillar_slug` CharField(max_length=255, blank=True) -- slug of the pillar page (for satellites to find their pillar)
       - `schema_type` CharField(max_length=50, blank=True, choices=[('TouristTrip','TouristTrip'),('Article','Article'),('FAQPage','FAQPage')]) -- which JSON-LD schema type to generate
       - `primary_keyword` CharField(max_length=255, blank=True, unique=True, null=True) -- MUST be unique+nullable (blank entries don't conflict). This is the keyword cannibalization guard.
       - Add `cluster_panels` list (like SEOMixin.seo_panels) with a MultiFieldPanel grouping all 5 fields under heading "SEO Cluster".
       - Add `cluster_api_fields` list with all 5 field names for easy APIField addition.
       - Meta: abstract = True

    2. In each page model file (rutas, blog, experiencias, cursos, ofertas):
       - Import ClusterMixin from apps.core.models
       - Add ClusterMixin to the model's inheritance chain (after SEOMixin, before HeadlessPreviewMixin)
       - Add `ClusterMixin.cluster_panels` to `promote_panels`
       - Add APIField entries for all 5 cluster fields (use the cluster_api_fields list pattern or add them individually)

    3. Generate Django migrations for each app. Run from the wagtail-headless-cms directory:
       ```
       cd ../wagtail-headless-cms && python manage.py makemigrations core rutas blog experiencias cursos ofertas
       ```
       NOTE: Do NOT run `migrate` -- migrations are created but applied manually by the user. The Next.js frontend must handle missing cluster data gracefully (all fields are optional/blank).

    IMPORTANT: The `primary_keyword` field must be `unique=True, null=True, blank=True`. Django treats NULL as distinct for uniqueness, so pages without a keyword won't conflict. This is the database-level cannibalization prevention (SEO-04 requirement).
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/wagtail-headless-cms && python -c "from apps.core.models import ClusterMixin; print('ClusterMixin fields:', [f.name for f in ClusterMixin._meta.get_fields()])" && python manage.py check --no-color 2>&1 | head -5</automated>
    <manual>Verify migration files were created in each app's migrations/ directory</manual>
  </verify>
  <done>ClusterMixin exists with 5 fields, all page models inherit it, API fields are declared, migrations are generated, Django check passes</done>
</task>

<task type="auto">
  <name>Task 2: Update Next.js TypeScript types for cluster metadata</name>
  <files>src/lib/wagtail/types.ts</files>
  <action>
    1. Add a `WagtailClusterFields` interface to `src/lib/wagtail/types.ts`:
       ```typescript
       export interface WagtailClusterFields {
         cluster_id?: string
         cluster_role?: 'pillar' | 'satellite'
         pillar_slug?: string
         schema_type?: 'TouristTrip' | 'Article' | 'FAQPage'
         primary_keyword?: string
       }
       ```
       All fields are optional (?) because existing pages won't have them until migrations run and data is populated.

    2. Add a `WagtailSEOFields` interface to capture the existing SEOMixin fields that are already returned by the API but not typed:
       ```typescript
       export interface WagtailSEOFields {
         meta_description?: string
         meta_keywords?: string
         social_title?: string
         social_description?: string
         social_image?: WagtailImage | null
       }
       ```

    3. Create a combined `WagtailPageWithSEO` type:
       ```typescript
       export type WagtailPageWithSEO = WagtailPage & WagtailSEOFields & WagtailClusterFields
       ```

    4. Update each existing page interface to extend `WagtailSEOFields & WagtailClusterFields` in addition to `WagtailPage`:
       - `WagtailBlogPostPage extends WagtailPage` -> add `& WagtailSEOFields & WagtailClusterFields` (or use intersection)
       - Same for WagtailRutaPage, WagtailExperienciaPage, WagtailOfertaPage, WagtailCursoPage, WagtailHomePage

    Do NOT break existing code -- all new fields are optional, so existing consumers continue to work unchanged.
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>WagtailClusterFields and WagtailSEOFields interfaces exist, all page types include them, TypeScript compiles without errors</done>
</task>

</tasks>

<verification>
1. Django system check passes in wagtail-headless-cms project
2. TypeScript compiles without errors in Next.js project
3. ClusterMixin has all 5 fields with correct constraints
4. All 6 page models (Home, Ruta, Blog, Experiencia, Curso, Oferta) inherit ClusterMixin
5. Migration files exist for each affected app
</verification>

<success_criteria>
- ClusterMixin abstract model exists in core/models.py with cluster_id, cluster_role, pillar_slug, schema_type, primary_keyword (unique+nullable)
- All page models inherit ClusterMixin and expose fields via API
- Django migrations generated (not applied)
- TypeScript types updated with optional cluster/SEO fields
- Both projects pass their respective type checks
</success_criteria>

<output>
After completion, create `.planning/phases/02-seo-content-pipeline/02-01-SUMMARY.md`
</output>
