---
phase: 08-wagtail-cms-migration
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - /home/mandycs/Proyectos/Clientes/Karlos/wagtail-headless-cms/apps/rutas/management/commands/populate_rutas.py
autonomous: false
requirements: [DATA-02, DATA-04]

must_haves:
  truths:
    - "5 real routes exist as published RutaPage instances in Wagtail CMS"
    - "Each route has correct price, duration, dive count, and itinerary days"
    - "Each route has real FAQs from the source data"
    - "Each route references correct Wagtail images (IDs 1-33)"
    - "The Wagtail API returns route data at /api/v2/pages/?type=rutas.RutaPage without 500 errors"
  artifacts:
    - path: "/home/mandycs/Proyectos/Clientes/Karlos/wagtail-headless-cms/apps/rutas/management/commands/populate_rutas.py"
      provides: "Django management command that creates/updates all 5 RutaPage instances with real data"
      contains: "class Command"
  key_links:
    - from: "populate_rutas command"
      to: "RutaPage model"
      via: "Django ORM page creation"
      pattern: "RutaPage.*add_child"
    - from: "Wagtail API"
      to: "RutaPage instances"
      via: "/api/v2/pages/?type=rutas.RutaPage"
      pattern: "rutas.RutaPage"
---

<objective>
Create a Django management command to populate all 5 real routes in Wagtail CMS with verified product data.

Purpose: Manually entering 5 routes x 8 itinerary days x FAQs x includes via the admin UI would be error-prone and slow. A management command is repeatable, correct, and can be re-run after model changes. This populates the CMS with the source of truth data from PRODUCT-DATA.md and the 5 route files.

Output: Management command that creates 5 published RutaPage instances with complete data. Routes accessible via Wagtail API.
</objective>

<execution_context>
@/home/mandycs/.claude/get-shit-done/workflows/execute-plan.md
@/home/mandycs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wagtail-cms-migration/08-RESEARCH.md
@.planning/phases/08-wagtail-cms-migration/08-01-SUMMARY.md
@.planning/seo/PRODUCT-DATA.md
@.planning/seo/routes/ruta-norte-pecios.md
@.planning/seo/routes/ruta-norte-tiran.md
@.planning/seo/routes/ruta-norte-brothers.md
@.planning/seo/routes/ruta-tiran-blue-hole.md
@.planning/seo/routes/ruta-sur-bde.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Django management command to populate 5 real routes</name>
  <files>
    /home/mandycs/Proyectos/Clientes/Karlos/wagtail-headless-cms/apps/rutas/management/__init__.py
    /home/mandycs/Proyectos/Clientes/Karlos/wagtail-headless-cms/apps/rutas/management/commands/__init__.py
    /home/mandycs/Proyectos/Clientes/Karlos/wagtail-headless-cms/apps/rutas/management/commands/populate_rutas.py
  </files>
  <action>
  Create the management command directory structure and `populate_rutas.py` in the Wagtail backend at `/home/mandycs/Proyectos/Clientes/Karlos/wagtail-headless-cms/`.

  Create empty `__init__.py` files for `management/` and `management/commands/` directories.

  The command should:

  1. **Find or create the HomePage** as parent page (RutaPage.parent_page_types = ['home.HomePage'])
  2. **For each of the 5 routes**, either update existing (by slug) or create new:
     - Delete existing RutaPage with same slug if it exists (to allow re-runs)
     - Create RutaPage as child of HomePage using `homepage.add_child(instance=ruta_page)`

  3. **Route data to populate** (read from `.planning/seo/routes/` files for full data, but hardcode in the command for portability):

  **Route 1: ruta-norte-pecios** (slug: "norte-pecios")
  - title: "Ruta Norte y Pecios del Mar Rojo"
  - hero_title: "Ruta Norte y Pecios del Mar Rojo desde Hurghada"
  - hero_subtitle: "Leyendas hundidas: explora los pecios mas miticos del Mar Rojo"
  - hero_badge: "RUTA NORTE Y PECIOS"
  - hero_background_image: Image ID 5 (wreck image)
  - story_intro_badge: "RUTA NORTE Y PECIOS"
  - story_intro_title: "Explora los pecios mas miticos del norte del Mar Rojo"
  - story_intro_content: (real description from ruta-norte-pecios.md)
  - info_cards: [{icon: "Calendar", value: "8 dias (7 noches)", label: "Duracion"}, {icon: "Waves", value: "16-18", label: "Inmersiones"}, {icon: "MapPin", value: "Hurghada", label: "Salida"}]
  - Price: 1.190 EUR
  - 8 itinerary days with real descriptions from route file
  - 12 incluye items from route file
  - 6 spots (Thistlegorm 14-30m, Ras Mohammed 5-40m, Giannis D 10-27m, Carnatic 10-28m, Dunraven 5-28m, Gubal/Ulysses 1-30m)
  - 9 real FAQs from route file
  - audience_fit with 3 profiles
  - practical_info with included/extras/logistics from route file
  - SEO fields: meta_description, primary_keyword "ruta norte pecios mar rojo"

  **Route 2: ruta-norte-tiran** (slug: "norte-tiran")
  - title: "Ruta Norte y Tiran del Mar Rojo"
  - Price: 1.190 EUR
  - Read full data from `.planning/seo/routes/ruta-norte-tiran.md`

  **Route 3: ruta-tiran-blue-hole** (slug: "tiran-blue-hole")
  - title: "Ruta Tiran y Blue Hole"
  - Price: 1.230 EUR
  - Read full data from `.planning/seo/routes/ruta-tiran-blue-hole.md`

  **Route 4: ruta-norte-brothers** (slug: "norte-brothers")
  - title: "Ruta Norte y Brothers"
  - Price: 1.230 EUR
  - Read full data from `.planning/seo/routes/ruta-norte-brothers.md`

  **Route 5: ruta-sur-bde** (slug: "sur-bde")
  - title: "Ruta Sur BDE (Brothers, Daedalus, Elphinstone)"
  - Price: 1.290 EUR
  - Read full data from `.planning/seo/routes/ruta-sur-bde.md`

  For each route:
  - Set `live=True` (published) and `has_unpublished_changes=False`
  - Create ItineraryDay orderables for each day (use `ruta_page.itinerary_days.create(...)` pattern or set via the Orderable relationship)
  - Assign Wagtail images by ID (1-33) based on semantic match:
    - Wrecks: IDs that match wreck photos
    - Underwater: IDs that match reef/marine life
    - Boat: IDs that match deck/boat photos
    - Use Image.objects.get(id=N) with try/except for safety
  - Set StreamField values as JSON lists of dicts with `type` and `value` keys (this is how Wagtail stores StreamField data)
  - Set CTA flat fields: cta_title, cta_description, cta_primary_text="Consultar esta ruta", cta_primary_link="#ruta-lead"
  - Set FAQ items, spots, audience profiles as StreamField JSON

  The command should accept a `--dry-run` flag to preview what would be created without writing to DB.
  The command should print progress for each route created.

  IMPORTANT: Read all 5 route data files from `.planning/seo/routes/` to extract the real itinerary, FAQs, includes, and spot data for each route. Each route file has a consistent structure with sections for itinerary, FAQ, includes, spots, etc.

  After creating the command, test it locally:
  ```bash
  cd /home/mandycs/Proyectos/Clientes/Karlos/wagtail-headless-cms
  source venv/bin/activate
  python manage.py populate_rutas --dry-run
  ```
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/wagtail-headless-cms && source venv/bin/activate && python manage.py populate_rutas --dry-run 2>&1 | head -30</automated>
    <manual>Verify the dry-run output shows 5 routes with correct data</manual>
  </verify>
  <done>Management command exists and dry-run shows 5 routes with real data (titles, prices, itinerary days, FAQs, spots) ready to be created in Wagtail.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify routes populated in Wagtail CMS</name>
  <action>Human verifies routes exist in Wagtail admin and API returns them.</action>
  <what-built>
  Django management command `populate_rutas` that creates 5 real route pages in Wagtail with verified product data from the route files.
  </what-built>
  <how-to-verify>
  1. **Prerequisite:** Plan 08-01 checkpoint must be completed (backend deployed with expanded RutaPage model).

  2. **Run the command on the deployed backend:**
     Either SSH into the Dokploy container or run locally against the production DB:
     ```bash
     cd /home/mandycs/Proyectos/Clientes/Karlos/wagtail-headless-cms
     source venv/bin/activate
     python manage.py populate_rutas
     ```

  3. **Verify via Wagtail Admin:**
     Visit `https://back.redsea.sphyrnasolutions.com/admin/` and check:
     - 5 RutaPage instances exist under HomePage
     - Each has correct title, itinerary days, FAQs, spots
     - Pages are published (green checkmark)

  4. **Verify via API:**
     ```bash
     curl -s "https://back.redsea.sphyrnasolutions.com/api/v2/pages/?type=rutas.RutaPage&fields=*" \
       -H "Authorization: Token 1abca6db19d5e58e0b9a2b5ee40d1a7be2ec55d9" | python3 -m json.tool | head -50
     ```
     Should return 5 pages without 500 errors.
  </how-to-verify>
  <resume-signal>Type "populated" once the 5 routes are visible in Wagtail admin and the API returns them, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
- `python manage.py populate_rutas --dry-run` completes without errors
- 5 RutaPage instances exist in Wagtail admin
- API endpoint `/api/v2/pages/?type=rutas.RutaPage` returns 200 with 5 items
- Each route has correct slug, title, price, and itinerary day count
- Each route has FAQs, spots, and includes populated
</verification>

<success_criteria>
1. All 5 routes published in Wagtail CMS with real product data
2. API returns route data without 500 errors (resolves known blocker)
3. Each route has complete data: itinerary, FAQs, includes, spots, audience fit
4. Routes reference correct Wagtail images
5. Command is re-runnable (can fix data and re-run)
</success_criteria>

<output>
After completion, create `.planning/phases/08-wagtail-cms-migration/08-02-SUMMARY.md`
</output>
