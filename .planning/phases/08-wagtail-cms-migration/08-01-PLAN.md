---
phase: 08-wagtail-cms-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/mandycs/Proyectos/Clientes/Karlos/wagtail-headless-cms/apps/rutas/models.py
  - /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte/src/lib/mock-data/homepage.ts
  - /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte/src/lib/mock-data/rutas.ts
autonomous: false
requirements: [DATA-01, DATA-03]

must_haves:
  truths:
    - "Homepage mock-data shows real product data (M/Y Dolce Vita 40m, 12 camarotes, 24 pax, desde 1.190 EUR)"
    - "Homepage mock-data shows real training data (Advanced SSI + 4 especialidades gratis + Nitrox incluido)"
    - "RutaPage Wagtail model has fields for all sections the frontend expects (FAQ, spots, summary, audience, practicalInfo)"
    - "RutaPage model includes cta flat fields instead of StreamField-only cta_section"
    - "Ruta Norte mock-data contains real verified product data (16-18 inmersiones, 8 dias, desde 1.190 EUR)"
  artifacts:
    - path: "/home/mandycs/Proyectos/Clientes/Karlos/wagtail-headless-cms/apps/rutas/models.py"
      provides: "Expanded RutaPage model with summary, spots, FAQ, audience, practicalInfo, resources, inline_lead sections + flat CTA fields"
      contains: "faq_section"
    - path: "/home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte/src/lib/mock-data/homepage.ts"
      provides: "Homepage with real product data from PRODUCT-DATA.md"
      contains: "1.190"
    - path: "/home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte/src/lib/mock-data/rutas.ts"
      provides: "Ruta Norte with real verified data (correct dives, prices, itinerary)"
      contains: "16-18"
  key_links:
    - from: "RutaPage model"
      to: "WagtailRutaPage TypeScript type"
      via: "API fields matching frontend expectations"
      pattern: "api_fields.*faq_section"
---

<objective>
Expand the Wagtail RutaPage model with missing fields and update mock-data with real product data.

Purpose: The RutaPage Django model is missing fields the frontend expects (FAQ, spots, summary, audience fit, practical info, resources, inline lead). The homepage and ruta mock-data contain placeholder/incorrect values that need replacing with verified real product data from PRODUCT-DATA.md and route files. This plan does the backend model expansion AND data correction simultaneously since they are independent codebases.

Output: Updated RutaPage model ready for migration + deploy, mock-data files with real product data.
</objective>

<execution_context>
@/home/mandycs/.claude/get-shit-done/workflows/execute-plan.md
@/home/mandycs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-wagtail-cms-migration/08-RESEARCH.md
@.planning/seo/PRODUCT-DATA.md
@.planning/seo/routes/ruta-norte-pecios.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand RutaPage Django model with missing sections and flat CTA fields</name>
  <files>
    /home/mandycs/Proyectos/Clientes/Karlos/wagtail-headless-cms/apps/rutas/models.py
  </files>
  <action>
  Expand the RutaPage model in the Wagtail backend codebase at `/home/mandycs/Proyectos/Clientes/Karlos/wagtail-headless-cms/`.

  Add the following fields to the RutaPage model (after the existing `incluye_items` field and before the `cta_section` field):

  1. **story_intro_badge** -- `CharField(max_length=100, blank=True)` for the badge text above story intro (the model already has `story_intro_title` and `story_intro_content`)

  2. **Summary Section** -- StreamField with a single `summary` StructBlock containing:
     - `eyebrow` (CharBlock, max_length=100)
     - `title` (CharBlock, max_length=255)
     - `subtitle` (TextBlock)
     - `bullets` (ListBlock of CharBlock)

  3. **Spots Section** -- Two flat fields + StreamField:
     - `spots_title` (CharField, max_length=255, default="Principales puntos de inmersion")
     - `spots_subtitle` (TextField, blank=True)
     - `spots` StreamField with `spot` StructBlock:
       - `name` (CharBlock)
       - `image` (ImageChooserBlock)
       - `alt` (CharBlock, required=False)
       - `summary` (TextBlock)
       - `depth` (CharBlock, max_length=50)
       - `tag` (CharBlock, max_length=100)

  4. **Audience Fit Section** -- Flat fields + StreamField:
     - `audience_fit_title` (CharField, max_length=255, default="Es esta ruta para ti?")
     - `audience_fit_subtitle` (TextField, blank=True)
     - `audience_fit_profiles` StreamField with `profile` StructBlock:
       - `title` (CharBlock)
       - `description` (TextBlock)
       - `tone` (ChoiceBlock: good-fit, consider, not-now)

  5. **Practical Info Section** -- Flat fields + StreamFields:
     - `practical_info_title` (CharField, max_length=255, default="Informacion practica")
     - `practical_info_subtitle` (TextField, blank=True)
     - `practical_info_included_title` (CharField, max_length=255, default="Incluido")
     - `practical_info_included` StreamField([('item', CharBlock)])
     - `practical_info_extras_title` (CharField, max_length=255, default="Extras")
     - `practical_info_extras` StreamField([('item', CharBlock)])
     - `practical_info_logistics_title` (CharField, max_length=255, default="Logistica")
     - `practical_info_logistics` StreamField([('item', CharBlock)])

  6. **FAQ Section** -- Flat field + StreamField:
     - `faq_title` (CharField, max_length=255, default="Preguntas frecuentes")
     - `faq_section` StreamField with `faq` StructBlock:
       - `question` (CharBlock, max_length=500)
       - `answer` (TextBlock)

  7. **Resources Section** -- Flat fields + StreamField:
     - `resources_title` (CharField, max_length=255, default="Guias relacionadas")
     - `resources_subtitle` (TextField, blank=True)
     - `resources_items` StreamField with `resource` StructBlock:
       - `title` (CharBlock)
       - `description` (TextBlock)
       - `href` (CharBlock, max_length=500)
       - `label` (CharBlock, max_length=100)

  8. **Replace cta_section StreamField with flat CTA fields** (the frontend mapper expects flat fields, not StreamField):
     - Keep `cta_section` for backward compat but add:
     - `cta_title` (CharField, max_length=255, blank=True)
     - `cta_description` (TextField, blank=True)
     - `cta_background_image` (ForeignKey to wagtailimages.Image, null=True, blank=True)
     - `cta_primary_text` (CharField, max_length=100, blank=True)
     - `cta_primary_link` (CharField, max_length=500, blank=True)

  9. **Inline Lead fields** (reuse pattern from homepage):
     - `inline_lead_section_id` (CharField, max_length=100, default="ruta-lead")
     - `inline_lead_eyebrow` (CharField, max_length=100, blank=True)
     - `inline_lead_title` (CharField, max_length=255, blank=True)
     - `inline_lead_subtitle` (TextField, blank=True)

  Add ALL new fields to `api_fields` list so the API exposes them.
  Add admin panels in collapsible MultiFieldPanels for each section.
  Add search_fields for new text fields (faq answer, spots summary).

  After modifying the model, run:
  ```bash
  cd /home/mandycs/Proyectos/Clientes/Karlos/wagtail-headless-cms
  source venv/bin/activate
  python manage.py makemigrations rutas
  ```

  Do NOT run `migrate` -- the user will deploy via Docker on Dokploy.
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/wagtail-headless-cms && source venv/bin/activate && python manage.py makemigrations --check --dry-run rutas 2>&1 | grep -q "No changes" && echo "FAIL: no migration generated" || echo "OK: migration file created" && ls apps/rutas/migrations/ | tail -1</automated>
    <manual>Verify the migration file was created in apps/rutas/migrations/</manual>
  </verify>
  <done>RutaPage model has all fields matching frontend RutaData type: story_intro_badge, summary section, spots section, audience fit, practical info, FAQ section, resources section, inline lead, and flat CTA fields. Migration file generated successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Update mock-data with real verified product data</name>
  <files>
    /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte/src/lib/mock-data/homepage.ts
    /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte/src/lib/mock-data/rutas.ts
  </files>
  <action>
  Update mock-data files in the Next.js frontend with REAL product data from `.planning/seo/PRODUCT-DATA.md` and `.planning/seo/routes/ruta-norte-pecios.md`.

  **homepage.ts changes:**

  1. Update `programSection.price`:
     - `amount`: "Desde 1.190 EUR" (was "Consultar")
     - `badge`: "MEJOR PRECIO" (was "FASE 1")
     - `perPerson`: "por persona" (was "segun salida")
     - `highlight`: "Advanced SSI + 4 especialidades + Nitrox incluidos" (was placeholder)

  2. Update `programSection.includes` with real data:
     - "7 noches de vida a bordo en el M/Y Dolce Vita"
     - "16-18 inmersiones con guias profesionales"
     - "Pension completa, refrescos, te y cafe"
     - "Advanced Open Water SSI gratuito a bordo"
     - "4 especialidades SSI incluidas"
     - "Nitrox (28%) incluido"
     - "Traslados aeropuerto-barco-aeropuerto"
     - "Seguro de viaje"

  3. Update `journeyOverview.highlights` with real data:
     - "7 noches a bordo del M/Y Dolce Vita (40m, 12 camarotes, 24 buceadores)"
     - "16-18 inmersiones en pecios legendarios y arrecifes"
     - "Salida desde Hurghada, traslados incluidos"

  4. Update `journeyOverview.includes`:
     - "Pension completa con refrescos, te, cafe y 2 cervezas/dia"
     - "Advanced SSI + 4 especialidades + seminarios gratuitos a bordo"
     - "Botella 12L, plomos y Nitrox (28%) incluidos"

  5. Update hero.subtitle: "Safari de buceo de 7 noches en el M/Y Dolce Vita entre pecios legendarios, arrecifes iconicos y formacion Advanced SSI gratuita a bordo. Desde 1.190 EUR."

  6. Update specSection.bigCard.subtitle: "7 noches desde 1.190 EUR | Advanced + 4 especialidades SSI gratis"

  **rutas.ts changes for "norte-7-dias":**

  1. Fix `infoCards`:
     - Duration: value "8 dias (7 noches)" (was "7 noches")
     - Dives: value "16-18" (was "18-22")
     - Keep Hurghada as salida

  2. Update `itinerary` to 8 days matching real data from `ruta-norte-pecios.md`:
     - Day 1: Embarque en Hurghada y bienvenida a bordo
     - Day 2: Inmersiones de chequeo y navegacion al norte
     - Day 3: Parque Nacional de Ras Mohamed y pecio SS Dunraven
     - Day 4: El mitico SS Thistlegorm
     - Day 5: Arrecife Abu Nuhas y sus tesoros hundidos
     - Day 6: Rosalie Moller y Ulysses
     - Day 7: Ultimas inmersiones y regreso al puerto
     - Day 8: Desembarque
     Use descriptions from the real route data. Assign appropriate images from `/images/` directory. Alternate overlayDirection left/right.

  3. Update `spotsSection.spots` with CORRECT depths from real data:
     - SS Thistlegorm: depth "14-30m" (was "16-32m")
     - Abu Nuhas/Giannis D: depth "10-27m" (was "4-26m")
     - Ras Mohammed: depth "5-40m" (was "10-35m")
     - Add Carnatic (10-28m), Dunraven (5-28m), Gubal/Ulysses (1-30m)

  4. Update `faqSection` with the 9 REAL FAQs from `ruta-norte-pecios.md` (replace the 5 generic ones)

  5. Update `incluye.items` with the 12 REAL items from the route data:
     - Traslados aeropuerto-barco-aeropuerto
     - 7 Noches de crucero
     - Pension completa a bordo
     - Refrescos, agua mineral, te y cafe
     - 2 cervezas por persona y dia
     - Guias de buceo locales
     - Tour Leader de nuestra agencia
     - 16/18 Inmersiones
     - Botella de 12L, plomos y NITROX (28%)
     - Seguro de viaje
     - Formacion avanzada gratuita a bordo
     - Seminarios gratuitos a bordo

  6. Update `storyIntro` with real content (remove internal marketing language):
     - badge: "RUTA NORTE Y PECIOS"
     - title: "Explora los pecios mas miticos del norte del Mar Rojo"
     - description: Use real description from ruta-norte-pecios.md (the opening paragraph)

  7. Update `cta` section with real copy:
     - title: "Reserva tu plaza en la Ruta Norte y Pecios"
     - description: "8 dias de buceo entre pecios legendarios desde Hurghada. Desde 1.190 EUR con Advanced SSI incluido."

  8. Update `practicalInfo` with real data:
     - included: real items from "Incluido en el precio" section
     - extras: real items from "No incluido en el precio" section
     - logistics: real logistics (Hurghada, embarque 16:00, check-out 10:00)

  Keep all field names, structure, and types exactly as they are. Only change VALUES.
  Do NOT change slugs, keys, or add/remove sections.
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && grep -c "1.190" src/lib/mock-data/homepage.ts && grep -c "16-18" src/lib/mock-data/rutas.ts && grep -c "14-30m" src/lib/mock-data/rutas.ts && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
    <manual>Verify homepage.ts has "1.190" EUR, rutas.ts has "16-18" inmersiones and "14-30m" Thistlegorm depth</manual>
  </verify>
  <done>Homepage mock-data shows real prices (desde 1.190 EUR), real boat specs (M/Y Dolce Vita), real training (Advanced SSI + 4 especialidades + Nitrox). Ruta Norte mock-data has correct 8-day itinerary, 16-18 dives, real depths, 9 real FAQs, 12 real includes, real no-includes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify deployment and data correctness</name>
  <action>Human verifies the deployed backend and frontend data.</action>
  <what-built>
  1. RutaPage Wagtail model expanded with FAQ, spots, summary, audience fit, practical info, resources, inline lead sections + flat CTA fields. Migration file generated.
  2. Homepage and Ruta Norte mock-data updated with real verified product data from PRODUCT-DATA.md.
  </what-built>
  <how-to-verify>
  1. **Frontend check:** Run `cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && npm run dev` and visit:
     - http://localhost:3000 -- verify homepage shows "Desde 1.190 EUR", "Advanced SSI + 4 especialidades", real boat data
     - http://localhost:3000/rutas/norte-7-dias -- verify 8-day itinerary, "16-18 inmersiones", 9 FAQs, correct depths
  2. **Backend check:** Verify migration file exists at `wagtail-headless-cms/apps/rutas/migrations/`
  3. **Deploy backend:** Deploy the Wagtail backend via Dokploy (Docker rebuild) to apply the migration. This creates the new fields in the production database.
  </how-to-verify>
  <resume-signal>Type "deployed" once the Wagtail backend is redeployed on Dokploy with the new migration applied, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors in frontend)
- `python manage.py makemigrations --check rutas` shows migration exists
- Homepage mock-data contains "1.190" EUR at least 2 times
- Ruta Norte mock-data contains "16-18" inmersiones, "14-30m" for Thistlegorm
- 9 FAQ items in ruta mock-data (was 5)
- 12 incluye items in ruta mock-data (was 6)
- 8 itinerary days in ruta mock-data (was 7)
</verification>

<success_criteria>
1. RutaPage Django model has all fields matching the frontend RutaData TypeScript type
2. Migration file generated and ready for deploy
3. Homepage shows real product data (prices, boat specs, training)
4. Ruta Norte shows real verified data (correct depths, FAQs, includes, itinerary)
5. Backend deployed to Dokploy with new migration applied
</success_criteria>

<output>
After completion, create `.planning/phases/08-wagtail-cms-migration/08-01-SUMMARY.md`
</output>
