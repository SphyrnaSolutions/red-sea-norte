---
phase: 10-seo-tooling-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - .env.example
  - .env.local
  - docker-compose.yml
autonomous: false
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03
user_setup:
  - service: google-search-console
    why: "Site verification and indexation monitoring"
    env_vars:
      - name: NEXT_PUBLIC_GSC_VERIFICATION
        source: "Google Search Console -> Add Property -> HTML tag method -> copy content value"
    dashboard_config:
      - task: "Add property buceoenelmarrojo.com"
        location: "Google Search Console -> Add Property -> URL prefix"
      - task: "Verify via HTML tag after deploy"
        location: "Google Search Console -> Verify"
  - service: bing-webmaster-tools
    why: "Bing/Yahoo search indexation"
    env_vars:
      - name: NEXT_PUBLIC_BING_VERIFICATION
        source: "Bing Webmaster Tools -> Add Site -> HTML Meta Tag method -> copy content value"
    dashboard_config:
      - task: "Add site buceoenelmarrojo.com"
        location: "Bing Webmaster Tools -> Add a Site"
      - task: "Verify via HTML meta tag after deploy"
        location: "Bing Webmaster Tools -> Verify"
  - service: google-analytics-4
    why: "Traffic measurement and Core Web Vitals"
    env_vars:
      - name: NEXT_PUBLIC_GA_MEASUREMENT_ID
        source: "Google Analytics -> Admin -> Data Streams -> Web -> Measurement ID (G-XXXXXXXXXX)"
    dashboard_config:
      - task: "Create GA4 property for buceoenelmarrojo.com"
        location: "Google Analytics -> Admin -> Create Property"
      - task: "Create Web data stream"
        location: "Google Analytics -> Admin -> Data Streams -> Add Stream -> Web"

must_haves:
  truths:
    - "La pagina HTML de buceoenelmarrojo.com contiene la meta tag de verificacion de Google Search Console"
    - "La pagina HTML de buceoenelmarrojo.com contiene la meta tag de verificacion de Bing Webmaster Tools"
    - "El script gtag.js de GA4 se carga en todas las paginas y envia pageviews al measurement ID configurado"
    - "Las verification tags y el GA4 measurement ID se configuran via env vars (no hardcoded)"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Verification meta tags and GA4 script loading"
      contains: "google-site-verification"
    - path: ".env.example"
      provides: "Documentation of required env vars for SEO tooling"
      contains: "NEXT_PUBLIC_GA_MEASUREMENT_ID"
  key_links:
    - from: "src/app/layout.tsx"
      to: "Google Analytics"
      via: "gtag.js script loaded with NEXT_PUBLIC_GA_MEASUREMENT_ID"
      pattern: "NEXT_PUBLIC_GA_MEASUREMENT_ID"
    - from: "src/app/layout.tsx"
      to: "Google Search Console"
      via: "meta tag google-site-verification"
      pattern: "google-site-verification"
    - from: "src/app/layout.tsx"
      to: "Bing Webmaster Tools"
      via: "meta tag msvalidate.01"
      pattern: "msvalidate\\.01"
---

<objective>
Install Google Analytics 4 tracking, Google Search Console verification meta tag, and Bing Webmaster Tools verification meta tag in the Next.js frontend. All values are configured via environment variables so they can be set per environment.

Purpose: Enable traffic measurement (GA4) and search engine verification (GSC + Bing) so the site can be monitored and indexed.
Output: Updated layout.tsx with GA4 script and verification meta tags, updated .env.example with new env vars.
</objective>

<execution_context>
@/home/mandycs/.claude/get-shit-done/workflows/execute-plan.md
@/home/mandycs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/layout.tsx
@.env.example
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install GA4 tag and search engine verification meta tags</name>
  <files>src/app/layout.tsx, .env.example, .env.local, docker-compose.yml</files>
  <action>
1. Install @next/third-parties package: `npm install @next/third-parties`

2. Update `src/app/layout.tsx`:
   - Import `GoogleAnalytics` from `@next/third-parties/google`
   - Add conditional Google Search Console verification meta tag in `<head>` using `process.env.NEXT_PUBLIC_GSC_VERIFICATION`. Only render the meta tag if the env var is set (truthy check). Tag format: `<meta name="google-site-verification" content="{value}" />`
   - Add conditional Bing Webmaster Tools verification meta tag in `<head>` using `process.env.NEXT_PUBLIC_BING_VERIFICATION`. Only render if set. Tag format: `<meta name="msvalidate.01" content="{value}" />`
   - Add `<GoogleAnalytics gaId={process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID!} />` just before `</body>` tag. Wrap in conditional: only render if `process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID` is set, so dev environments without GA4 configured don't break.
   - Do NOT use Script component manually -- @next/third-parties handles gtag loading optimally with built-in consent and performance patterns.

3. Update `.env.example` -- append these lines at the end:
   ```
   # SEO Tooling (Phase 10)
   NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
   NEXT_PUBLIC_GSC_VERIFICATION=your-gsc-verification-code
   NEXT_PUBLIC_BING_VERIFICATION=your-bing-verification-code
   ```

4. Update `.env.local` -- append same 3 env vars with empty/placeholder values so dev build works.

5. Update `docker-compose.yml` -- add the 3 new env vars to the build args section (they are NEXT_PUBLIC_ so they need to be available at build time). Add them alongside the existing `NEXT_PUBLIC_WAGTAIL_URL` and `NEXT_PUBLIC_SITE_HOSTNAME` build args. Also add them to the `environment` section for runtime access.

6. Run `npm run build` to verify the build succeeds with the new package and env vars.
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && npm run build 2>&1 | tail -5</automated>
    <manual>Verify layout.tsx contains google-site-verification meta tag, msvalidate.01 meta tag, and GoogleAnalytics component</manual>
  </verify>
  <done>
    - @next/third-parties installed in package.json
    - layout.tsx renders GoogleAnalytics component conditionally when NEXT_PUBLIC_GA_MEASUREMENT_ID is set
    - layout.tsx renders google-site-verification meta tag conditionally when NEXT_PUBLIC_GSC_VERIFICATION is set
    - layout.tsx renders msvalidate.01 meta tag conditionally when NEXT_PUBLIC_BING_VERIFICATION is set
    - .env.example documents all 3 new env vars
    - docker-compose.yml includes the 3 new env vars as build args
    - Build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify search engine registration and GA4 tracking</name>
  <files>n/a</files>
  <action>
Human verification checkpoint. Claude has installed all code in Task 1. The user must now:
1. Create accounts/properties in Google Search Console, Bing Webmaster Tools, and Google Analytics 4
2. Get the verification codes and GA4 measurement ID from each dashboard
3. Set the env vars in production (Dokploy) and redeploy
4. Complete verification in each dashboard

Detailed steps below in verify section.
  </action>
  <verify>
    <automated>curl -s https://buceoenelmarrojo.com | grep -c "google-site-verification\|msvalidate.01\|gtag"</automated>
    <manual>
    1. Google Analytics 4:
       - Go to https://analytics.google.com -> Admin -> Create Property for buceoenelmarrojo.com
       - Create a Web data stream, copy the Measurement ID (G-XXXXXXXXXX)
       - Set NEXT_PUBLIC_GA_MEASUREMENT_ID in production env vars and redeploy
       - Visit buceoenelmarrojo.com, then check GA4 Realtime report -- should show 1 active user
    2. Google Search Console:
       - Go to https://search.google.com/search-console -> Add Property -> URL prefix -> enter https://buceoenelmarrojo.com
       - Select HTML tag verification method, copy the content value
       - Set NEXT_PUBLIC_GSC_VERIFICATION in production env vars and redeploy
       - Return to GSC and click Verify -- should show Ownership verified
    3. Bing Webmaster Tools:
       - Go to https://www.bing.com/webmasters -> Add Site -> enter https://buceoenelmarrojo.com
       - Select HTML Meta Tag verification, copy the content value
       - Set NEXT_PUBLIC_BING_VERIFICATION in production env vars and redeploy
       - Return to Bing and click Verify -- should show site as verified
    </manual>
  </verify>
  <done>
    - buceoenelmarrojo.com verified in Google Search Console (Ownership verified status)
    - buceoenelmarrojo.com verified in Bing Webmaster Tools (site verified status)
    - GA4 Realtime report shows active pageviews when visiting buceoenelmarrojo.com
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. `grep -c "google-site-verification\|msvalidate.01\|GoogleAnalytics" src/app/layout.tsx` returns 3+ matches
3. `grep -c "NEXT_PUBLIC_GA_MEASUREMENT_ID\|NEXT_PUBLIC_GSC_VERIFICATION\|NEXT_PUBLIC_BING_VERIFICATION" .env.example` returns 3
4. After production deploy with real values: GA4 Realtime shows pageviews, GSC shows verified, Bing shows verified
</verification>

<success_criteria>
- GA4 gtag loads on all pages and sends pageview events (visible in GA4 Realtime)
- buceoenelmarrojo.com appears as verified property in Google Search Console
- buceoenelmarrojo.com appears as verified site in Bing Webmaster Tools
- All config is via env vars, no hardcoded IDs in source code
</success_criteria>

<output>
After completion, create `.planning/phases/10-seo-tooling-setup/10-01-SUMMARY.md`
</output>
