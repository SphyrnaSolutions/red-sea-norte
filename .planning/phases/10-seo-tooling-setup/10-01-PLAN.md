---
phase: 10-seo-tooling-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/components/CookieConsent.tsx
  - .env.example
  - .env.local
  - docker-compose.yml
autonomous: false
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03
user_setup:
  - service: google-search-console
    why: "Site verification and indexation monitoring"
    env_vars:
      - name: NEXT_PUBLIC_GSC_VERIFICATION
        source: "Google Search Console -> Add Property -> HTML tag method -> copy content value"
    dashboard_config:
      - task: "Add property buceoenelmarrojo.com"
        location: "Google Search Console -> Add Property -> URL prefix"
      - task: "Verify via HTML tag after deploy"
        location: "Google Search Console -> Verify"
  - service: bing-webmaster-tools
    why: "Bing/Yahoo search indexation"
    env_vars:
      - name: NEXT_PUBLIC_BING_VERIFICATION
        source: "Bing Webmaster Tools -> Add Site -> HTML Meta Tag method -> copy content value"
    dashboard_config:
      - task: "Add site buceoenelmarrojo.com"
        location: "Bing Webmaster Tools -> Add a Site"
      - task: "Verify via HTML meta tag after deploy"
        location: "Bing Webmaster Tools -> Verify"
  - service: google-analytics-4
    why: "Traffic measurement and Core Web Vitals"
    env_vars:
      - name: NEXT_PUBLIC_GA_MEASUREMENT_ID
        source: "Google Analytics -> Admin -> Data Streams -> Web -> Measurement ID (G-XXXXXXXXXX)"
    dashboard_config:
      - task: "Create GA4 property for buceoenelmarrojo.com"
        location: "Google Analytics -> Admin -> Create Property"
      - task: "Create Web data stream"
        location: "Google Analytics -> Admin -> Data Streams -> Add Stream -> Web"

must_haves:
  truths:
    - "La pagina HTML de buceoenelmarrojo.com contiene la meta tag de verificacion de Google Search Console"
    - "La pagina HTML de buceoenelmarrojo.com contiene la meta tag de verificacion de Bing Webmaster Tools"
    - "El script gtag.js de GA4 solo se carga DESPUES de que el usuario acepte cookies (Google Consent Mode v2)"
    - "Existe un banner de cookies que permite aceptar o rechazar cookies analiticas, cumpliendo GDPR/LSSI"
    - "Las verification tags y el GA4 measurement ID se configuran via env vars (no hardcoded)"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Verification meta tags and GA4 script with consent mode"
      contains: "google-site-verification"
    - path: "src/components/CookieConsent.tsx"
      provides: "Cookie consent banner with accept/reject and localStorage persistence"
      contains: "cookie"
    - path: ".env.example"
      provides: "Documentation of required env vars for SEO tooling"
      contains: "NEXT_PUBLIC_GA_MEASUREMENT_ID"
  key_links:
    - from: "src/components/CookieConsent.tsx"
      to: "Google Analytics"
      via: "gtag consent update on user accept"
      pattern: "consent.*update"
    - from: "src/app/layout.tsx"
      to: "Google Analytics"
      via: "GoogleAnalytics component with consent mode default denied"
      pattern: "NEXT_PUBLIC_GA_MEASUREMENT_ID"
    - from: "src/app/layout.tsx"
      to: "Google Search Console"
      via: "meta tag google-site-verification"
      pattern: "google-site-verification"
    - from: "src/app/layout.tsx"
      to: "Bing Webmaster Tools"
      via: "meta tag msvalidate.01"
      pattern: "msvalidate\\.01"
---

<objective>
Install Google Analytics 4 with Consent Mode v2, a GDPR-compliant cookie consent banner, Google Search Console verification meta tag, and Bing Webmaster Tools verification meta tag in the Next.js frontend.

GA4 must NOT set cookies until the user explicitly accepts via the cookie banner (Google Consent Mode v2: default denied, update to granted on accept). Verification meta tags (GSC, Bing) do not use cookies and render unconditionally.

All tracking IDs are configured via environment variables.

Purpose: Enable traffic measurement (GA4 with GDPR compliance) and search engine verification (GSC + Bing).
Output: Cookie consent banner component, updated layout.tsx with consent-gated GA4 and verification meta tags, updated .env.example.
</objective>

<execution_context>
@/home/mandycs/.claude/get-shit-done/workflows/execute-plan.md
@/home/mandycs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/layout.tsx
@.env.example
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install GA4 with Consent Mode v2 and cookie consent banner</name>
  <files>src/app/layout.tsx, src/components/CookieConsent.tsx, .env.example, .env.local, docker-compose.yml</files>
  <action>
1. Install @next/third-parties package: `npm install @next/third-parties`

2. Create `src/components/CookieConsent.tsx` (client component):
   - "use client" directive at top
   - On mount, check `localStorage.getItem("cookie-consent")`:
     - If "accepted": call `gtag("consent", "update", { analytics_storage: "granted" })` and don't show banner
     - If "rejected": don't show banner, GA4 stays in denied mode (cookieless pings only)
     - If null (no choice yet): show the banner
   - Banner UI:
     - Fixed position bottom of screen, full width
     - Text: "Usamos cookies analiticas para mejorar tu experiencia. Puedes aceptar o rechazar su uso."
     - Link to /politica-de-privacidad
     - Two buttons: "Aceptar" (primary) and "Rechazar" (secondary)
   - On "Aceptar": `localStorage.setItem("cookie-consent", "accepted")`, call `gtag("consent", "update", { analytics_storage: "granted" })`, hide banner
   - On "Rechazar": `localStorage.setItem("cookie-consent", "rejected")`, hide banner (GA4 stays denied)
   - Style with Tailwind classes matching site's existing dark theme aesthetic
   - Declare `gtag` function type at component level: `declare function gtag(...args: any[]): void`

3. Update `src/app/layout.tsx`:
   - Import `GoogleAnalytics` from `@next/third-parties/google`
   - Import `CookieConsent` from `@/components/CookieConsent`
   - Add a `<Script>` tag (from next/script) BEFORE GoogleAnalytics to set Consent Mode v2 defaults:
     ```
     <Script id="gtag-consent-default" strategy="beforeInteractive">
     {`window.dataLayer = window.dataLayer || [];
     function gtag(){dataLayer.push(arguments);}
     gtag('consent', 'default', {
       'analytics_storage': 'denied',
       'ad_storage': 'denied',
       'ad_user_data': 'denied',
       'ad_personalization': 'denied'
     });`}
     </Script>
     ```
   - Add conditional GoogleAnalytics component: only render if `process.env.NEXT_PUBLIC_GA_MEASUREMENT_ID` is set
   - Add `<CookieConsent />` before closing `</body>`
   - Add conditional Google Search Console verification meta tag in `<head>`: `<meta name="google-site-verification" content="{value}" />` (only if NEXT_PUBLIC_GSC_VERIFICATION is set)
   - Add conditional Bing verification meta tag in `<head>`: `<meta name="msvalidate.01" content="{value}" />` (only if NEXT_PUBLIC_BING_VERIFICATION is set)

4. Update `.env.example` -- append:
   ```
   # SEO Tooling (Phase 10)
   NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
   NEXT_PUBLIC_GSC_VERIFICATION=your-gsc-verification-code
   NEXT_PUBLIC_BING_VERIFICATION=your-bing-verification-code
   ```

5. Update `.env.local` -- append same 3 env vars with empty/placeholder values.

6. Update `docker-compose.yml` -- add the 3 new env vars to build args and environment sections alongside existing NEXT_PUBLIC_ vars.

7. Run `npm run build` to verify the build succeeds.
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && npm run build 2>&1 | tail -5</automated>
    <manual>Verify layout.tsx contains consent mode default denied script, GoogleAnalytics component, verification meta tags, and CookieConsent component</manual>
  </verify>
  <done>
    - @next/third-parties installed in package.json
    - CookieConsent.tsx created with accept/reject buttons and localStorage persistence
    - Consent Mode v2 defaults to denied for all storage types
    - GA4 only tracks with cookies after user clicks "Aceptar"
    - layout.tsx renders GoogleAnalytics component conditionally
    - layout.tsx renders google-site-verification meta tag conditionally
    - layout.tsx renders msvalidate.01 meta tag conditionally
    - .env.example documents all 3 new env vars
    - docker-compose.yml includes the 3 new env vars
    - Build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify search engine registration, GA4 tracking, and cookie consent</name>
  <files>n/a</files>
  <action>
Human verification checkpoint. Claude has installed all code in Task 1. The user must now:
1. Create accounts/properties in Google Search Console, Bing Webmaster Tools, and Google Analytics 4
2. Get the verification codes and GA4 measurement ID from each dashboard
3. Set the env vars in production (Dokploy) and redeploy
4. Complete verification in each dashboard
5. Test cookie banner: visit site, verify banner appears, accept cookies, check GA4 Realtime
  </action>
  <verify>
    <automated>curl -s https://buceoenelmarrojo.com | grep -c "google-site-verification\|msvalidate.01\|gtag\|consent"</automated>
    <manual>
    1. Google Analytics 4:
       - Go to https://analytics.google.com -> Admin -> Create Property for buceoenelmarrojo.com
       - Create a Web data stream, copy the Measurement ID (G-XXXXXXXXXX)
       - Set NEXT_PUBLIC_GA_MEASUREMENT_ID in production env vars and redeploy
       - Visit buceoenelmarrojo.com, accept cookies in the banner
       - Check GA4 Realtime report -- should show 1 active user
    2. Google Search Console:
       - Go to https://search.google.com/search-console -> Add Property -> URL prefix -> enter https://buceoenelmarrojo.com
       - Select HTML tag verification method, copy the content value
       - Set NEXT_PUBLIC_GSC_VERIFICATION in production env vars and redeploy
       - Return to GSC and click Verify -- should show Ownership verified
    3. Bing Webmaster Tools:
       - Go to https://www.bing.com/webmasters -> Add Site -> enter https://buceoenelmarrojo.com
       - Select HTML Meta Tag verification, copy the content value
       - Set NEXT_PUBLIC_BING_VERIFICATION in production env vars and redeploy
       - Return to Bing and click Verify -- should show site as verified
    4. Cookie Consent:
       - Visit site in incognito -- banner should appear at bottom
       - Click "Rechazar" -- banner disappears, GA4 does NOT set cookies (check DevTools Application > Cookies)
       - Clear localStorage, reload -- banner appears again
       - Click "Aceptar" -- banner disappears, GA4 cookies appear (_ga, _ga_*)
       - Reload page -- banner does NOT reappear (choice persisted)
    </manual>
  </verify>
  <done>
    - buceoenelmarrojo.com verified in Google Search Console (Ownership verified status)
    - buceoenelmarrojo.com verified in Bing Webmaster Tools (site verified status)
    - GA4 Realtime report shows active pageviews when visiting buceoenelmarrojo.com after accepting cookies
    - Cookie banner appears on first visit, persists choice, and correctly gates GA4 cookie storage
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. `grep -c "google-site-verification\|msvalidate.01\|GoogleAnalytics\|CookieConsent" src/app/layout.tsx` returns 4+ matches
3. `grep -c "consent.*update\|analytics_storage\|cookie-consent" src/components/CookieConsent.tsx` returns 3+ matches
4. `grep -c "NEXT_PUBLIC_GA_MEASUREMENT_ID\|NEXT_PUBLIC_GSC_VERIFICATION\|NEXT_PUBLIC_BING_VERIFICATION" .env.example` returns 3
5. After production deploy: cookie banner appears, GA4 only tracks after accept, GSC verified, Bing verified
</verification>

<success_criteria>
- Cookie consent banner appears on first visit with accept/reject options
- GA4 Consent Mode v2 defaults to denied -- no cookies set until user accepts
- After accepting: GA4 gtag loads fully and sends pageview events (visible in GA4 Realtime)
- After rejecting: GA4 sends cookieless pings only, no _ga cookies set
- buceoenelmarrojo.com appears as verified property in Google Search Console
- buceoenelmarrojo.com appears as verified site in Bing Webmaster Tools
- All config is via env vars, no hardcoded IDs in source code
- Compliant with GDPR and LSSI-CE (Spanish cookie law)
</success_criteria>

<output>
After completion, create `.planning/phases/10-seo-tooling-setup/10-01-SUMMARY.md`
</output>
