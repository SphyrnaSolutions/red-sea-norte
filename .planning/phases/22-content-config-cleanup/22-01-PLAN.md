---
phase: 22-content-config-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/app/rutas/[slug]/page.tsx
  - src/app/rutas/page.tsx
  - src/lib/wagtail/mappers.ts
  - src/lib/mock-data/homepage.ts
  - src/app/robots.ts
  - src/app/sitemap/pages/route.ts
  - src/lib/data/rutas.ts
  - src/lib/mock-data/types.ts
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "No internal marketing notes are visible on any public page (no 'enfoque comercial', 'ruta comercial pensada para', 'pedir el lead', etc.)"
    - "robots.txt has exactly one User-agent: * block with no duplicates"
    - "Rutas sitemap includes lastmod dates derived from CMS last_published_at"
    - "Pages sitemap includes lastmod dates (fixed known date for static pages)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage metadata without internal marketing language"
      contains: "description:"
    - path: "src/app/rutas/[slug]/page.tsx"
      provides: "Ruta detail page without internal strategy notes in fallback content"
    - path: "src/app/robots.ts"
      provides: "Clean robots.txt with single User-agent block"
      exports: ["default"]
    - path: "src/app/sitemap/pages/route.ts"
      provides: "Pages sitemap with lastmod dates"
      contains: "lastmod"
    - path: "src/lib/data/rutas.ts"
      provides: "Rutas slug data with lastModified from CMS"
      contains: "lastPublishedAt"
  key_links:
    - from: "src/lib/data/rutas.ts"
      to: "src/app/sitemap/rutas/route.ts"
      via: "getAllRutasSlugsData returns lastModified"
      pattern: "lastModified.*last_published_at|lastPublishedAt"
    - from: "src/app/sitemap/pages/route.ts"
      to: "sitemap output"
      via: "lastmod element in XML"
      pattern: "<lastmod>"
---

<objective>
Clean up internal marketing notes from public-facing content, deduplicate robots.txt, and add lastmod dates to rutas and pages sitemaps.

Purpose: Eliminate SEO-damaging internal strategy notes visible to Google and users, fix robots.txt duplication from Cloudflare, and improve sitemap quality with proper lastmod dates.
Output: Clean public content, single-block robots.txt, sitemaps with dates.
</objective>

<execution_context>
@/home/mandycs/.claude/get-shit-done/workflows/execute-plan.md
@/home/mandycs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove internal marketing notes from public content</name>
  <files>
    src/app/page.tsx
    src/app/rutas/[slug]/page.tsx
    src/app/rutas/page.tsx
    src/lib/wagtail/mappers.ts
    src/lib/mock-data/homepage.ts
  </files>
  <action>
    Remove or rewrite all internal marketing/strategy language from public-facing code. These are notes written for the developer/marketer that leaked into user-visible text.

    **src/app/page.tsx line 21** — Replace meta description:
    - Current: "...enfoque comercial claro y opcion de Advanced SSI a bordo."
    - Replace with user-facing language, e.g.: "...spots iconicos como Thistlegorm y Abu Nuhas, y opcion de Advanced SSI a bordo."

    **src/app/rutas/[slug]/page.tsx lines 186-188** — Replace fallback bullets in getSummarySection():
    - Remove: "Ruta comercial pensada para explicar producto y encaje antes de pedir el lead"
    - Replace with user-facing bullet, e.g.: "Itinerario disenado para combinar los mejores pecios y arrecifes en una semana"
    - Line 188 "Salida desde Hurghada con narrativa clara de viaje" — rewrite to: "Salida desde Hurghada con un plan de viaje claro dia a dia"

    **src/app/rutas/[slug]/page.tsx line 198** — Replace subtitle in getSpotsSection():
    - Remove: "La pagina debe explicar que spots justifican esta ruta y por que."
    - Replace with: "Descubre los puntos de inmersion que hacen unica esta ruta."

    **src/app/rutas/[slug]/page.tsx line 214** — Replace subtitle in getPracticalInfo():
    - Remove: "Bloque de apoyo mientras Wagtail mantiene el esquema actual."
    - Replace with: "Todo lo que necesitas saber antes de reservar."

    **src/app/rutas/page.tsx line 12** — Replace meta description:
    - Remove "con intencion comercial clara" from description
    - Replace with user-facing language, e.g.: "...experiencias de buceo con itinerarios detallados dia a dia."

    **src/app/rutas/page.tsx line 127** — Replace visible text:
    - Remove "de vida a bordo con un siguiente paso comercial claro"
    - Replace with: "de vida a bordo con toda la informacion para elegir tu proxima aventura"

    **src/lib/wagtail/mappers.ts line 293** — Remove internal note:
    - Remove: "Esta home sintetiza la experiencia y deja la profundizacion comercial para el cluster y las landings."
    - Replace with empty string or a neutral description.

    **src/lib/wagtail/mappers.ts lines 308-322** — Clean audience fit fallback profiles:
    - Line 308: "Bloque derivado mientras Wagtail mantiene el esquema anterior de homepage." -> "Comprueba si esta experiencia es para ti."
    - Line 311: "Buscas una ruta comercial clara por el Mar Rojo" -> "Buscas una ruta definida por el Mar Rojo"
    - Line 312: "Usuario interesado en vida a bordo..." -> "Te interesa vida a bordo con spots reconocidos y una propuesta clara."
    - Line 322: "La homepage ya no debe vender exclusivamente certificacion como producto principal." -> "La formacion es un complemento del viaje, no el producto principal."

    **src/lib/mock-data/homepage.ts** — Clean internal notes (lines 114, 137, 293, 326):
    - Line 114: remove "intencion comercial" language, rewrite to user-facing
    - Line 137: "clasico comercial" -> "clasico del Mar Rojo"
    - Line 293: "foco comercial claro" -> "foco en los mejores spots"
    - Line 326: "valor, spots, encaje y lead" -> "valor, spots y experiencia"

    Do NOT touch src/lib/mock-data/blog-posts.ts — that file is mock-data for development and the blog content comes from Wagtail CMS in production. Also do NOT touch src/app/politica-de-privacidad/page.tsx — "comercial" there refers to legal "nombre comercial" and "comunicaciones comerciales" which are correct legal terms.
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && grep -rn "enfoque comercial\|ruta comercial pensada\|pedir el lead\|intencion comercial\|foco comercial\|paso comercial claro\|esquema anterior\|esquema actual\|Bloque de apoyo mientras\|Bloque derivado mientras\|La pagina debe explicar" src/app/ src/lib/wagtail/mappers.ts src/lib/mock-data/homepage.ts | grep -v node_modules | grep -v ".next" ; test $? -eq 1</automated>
    <manual>Verify no internal marketing notes visible by checking the grep returns zero matches</manual>
  </verify>
  <done>No internal marketing/strategy notes visible in any public-facing source file (page components, metadata, mappers, homepage mock data). Legal uses of "comercial" in privacy policy are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Deduplicate robots.txt and add lastmod to sitemaps</name>
  <files>
    src/app/robots.ts
    src/app/sitemap/pages/route.ts
    src/lib/data/rutas.ts
    src/lib/mock-data/types.ts
  </files>
  <action>
    **CLEAN-02: robots.txt deduplication**

    The current src/app/robots.ts already generates a clean single User-agent block. The duplicate comes from Cloudflare injecting its own robots.txt rules. Verify the current file is correct (it is — single `userAgent: '*'` block). Add a comment at the top explaining that Cloudflare must NOT have its own robots.txt rule enabled (the Cloudflare "managed" robots.txt feature should be OFF). The Next.js robots.ts file is the single source of truth.

    No code change needed to robots.ts itself — it already has exactly one block. The "duplication" is a Cloudflare config issue. Add a comment documenting this:
    ```
    // NOTE: Cloudflare must NOT inject its own robots.txt (disable "Managed robots.txt" in Cloudflare).
    // This file is the single source of truth for robots.txt.
    ```

    **CLEAN-03: Lastmod in rutas sitemap**

    In src/lib/data/rutas.ts, the `getAllRutasSlugsData` function currently returns `lastModified: undefined`. The Wagtail API response already includes `meta.last_published_at` on every page (confirmed in src/lib/wagtail/client.ts WagtailPageMeta interface). The ruta objects returned by `getAllRutas` come from `mapRutaPage` which does NOT include this field.

    Fix: In `getAllRutasSlugsData`, instead of mapping the already-mapped RutaData objects, access the raw Wagtail data. However, since `getAllRutas` returns mapped data, the simplest fix is:

    1. Add `lastPublishedAt?: string` to the `RutaData` interface in src/lib/mock-data/types.ts
    2. In src/lib/wagtail/mappers.ts `mapRutaPage`, add to the return object:
       ```
       lastPublishedAt: wagtailPage.meta.last_published_at || wagtailPage.meta.first_published_at,
       ```
    3. In src/lib/data/rutas.ts `getAllRutasSlugsData`, change `lastModified: undefined` to:
       ```
       lastModified: r.lastPublishedAt || undefined,
       ```
    4. Do the same for the mock fallback path — use a fixed date '2026-03-08' as fallback.

    **CLEAN-03: Lastmod in pages sitemap**

    In src/app/sitemap/pages/route.ts, the static pages have no lastmod. Add a fixed lastmod date for each static page. Use '2026-03-08' (current date, when these pages were last meaningfully updated). Update the XML generation to include `<lastmod>2026-03-08</lastmod>` for each static page URL.

    Replace the simple string mapping with an array of objects:
    ```typescript
    const staticPages = [
      { loc: BASE_URL, lastmod: '2026-03-08' },
      { loc: `${BASE_URL}/blog`, lastmod: '2026-03-08' },
      { loc: `${BASE_URL}/rutas`, lastmod: '2026-03-08' },
      { loc: `${BASE_URL}/cursos`, lastmod: '2026-03-08' },
      { loc: `${BASE_URL}/experiencias`, lastmod: '2026-03-08' },
      { loc: `${BASE_URL}/politica-de-privacidad`, lastmod: '2026-03-08' },
    ]
    ```
    And update the XML template to include `<lastmod>${page.lastmod}</lastmod>`.
  </action>
  <verify>
    <automated>cd /home/mandycs/Proyectos/Clientes/Karlos/red-sea-norte && npx tsc --noEmit 2>&1 | head -20</automated>
    <manual>Check that: (1) robots.ts has the Cloudflare comment, (2) pages sitemap includes lastmod, (3) rutas data layer passes lastPublishedAt through</manual>
  </verify>
  <done>robots.txt has single User-agent block with documentation comment about Cloudflare. Pages sitemap includes lastmod dates. Rutas data layer passes lastPublishedAt from CMS through to sitemap generation. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. `grep -rn "enfoque comercial\|ruta comercial pensada\|pedir el lead" src/app/ src/lib/wagtail/ src/lib/mock-data/homepage.ts` returns zero matches
2. `curl -s https://buceoenelmarrojo.com/robots.txt` shows single User-agent: * block (post-deploy)
3. `curl -s https://buceoenelmarrojo.com/sitemap/pages` shows lastmod on all URLs
4. `curl -s https://buceoenelmarrojo.com/sitemap/rutas` shows lastmod on all ruta URLs
5. TypeScript compiles without errors: `npx tsc --noEmit`
</verification>

<success_criteria>
- Zero internal marketing/strategy notes in public-facing source code (excluding mock blog data and legal pages)
- robots.ts documents Cloudflare dedup requirement and generates single User-agent block
- All 6 pages sitemap URLs include lastmod dates
- All rutas sitemap URLs include lastmod dates derived from Wagtail CMS last_published_at
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/22-content-config-cleanup/22-01-SUMMARY.md`
</output>
